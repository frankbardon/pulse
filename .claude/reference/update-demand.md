# Update Demand — Full Trigger Table

CLAUDE.md carries the compressed one-row-per-category version of this table — enough for the non-skippable gates and skim reading. This file is the exhaustive list: every Pulse-internal contract that has its own trigger row.

Rule unchanged: any change in column 1 MUST also update everything in column 2, in the SAME PR. CI fails otherwise.

| If you change... | You MUST also update... | Enforced by |
|---|---|---|
| A registered aggregator | `skills/op-agg-<kebab>.md` (atomic skill) + `descriptor/capabilities_aggregators.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllComponents`, `TestManifestOperatorsComplete` |
| A registered attribute | `skills/op-attr-<kebab>.md` (atomic skill) + `descriptor/capabilities_attributes.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllComponents`, `TestManifestOperatorsComplete` |
| A registered filterer | `skills/op-filter-<kebab>.md` (atomic skill) + `descriptor/capabilities_filterers.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllComponents`, `TestManifestOperatorsComplete` |
| A registered grouper | `skills/op-group-<kebab>.md` (atomic skill) + `descriptor/capabilities_groupers.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllComponents`, `TestManifestOperatorsComplete` |
| `Group.Include` slot (inclusion list) | `skills/grouper-design.md` (Group.Include section) + `processing/grouper.go` + `processing/grouper_set.go` | `TestGroupSetPerElement_IncludeFilters`, `TestGroupSetValue_IncludeFiltersCompositeKey`, `TestGroupCategory_IncludeFiltersLabels` |
| A registered window operator | `skills/op-win-<kebab>.md` (atomic skill) + `descriptor/capabilities_windows.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllWindowTypes`, `TestManifestOperatorsComplete` |
| A registered feature operator | `skills/op-feat-<kebab>.md` (atomic skill) + `descriptor/capabilities_features.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllComponents`, `TestManifestOperatorsComplete` |
| A registered statistical test (`TEST_*`) or tier-2 variant | `skills/op-test-<kebab>.md` (atomic skill) + `types/streamability.go` + `descriptor/capabilities_tests.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestStreamability_TestsKnown`, `TestManifestTestsComplete` |
| A registered regression (`REG_*`) or modifier | `skills/op-reg-<kebab>.md` (atomic skill; modifiers ship as `skills/op-reg-mod-<kebab>.md`) + `descriptor/capabilities_regressions.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllRegressions`, `TestManifestRegressionsComplete` |
| A registered synth distribution | `skills/op-synth-<kebab>.md` (atomic skill) + `descriptor/capabilities_distributions.go` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllSynthDistributions`, `TestManifestDistributionsComplete` |
| An operator's streaming capability | `types/streamability.go` + `types/streamability_test.go` | `TestRegistryStreamabilityMatchesTypes`, `TestManifestStreamableMatchesTypes` |
| An error code (add/remove/rename) | `errors/fixup_metadata.go` (`codeMetadata`) — Message + Fixups | `TestCodesHaveFixups`, `TestManifestErrorCodesComplete` |
| A CLI leaf (add/remove/flag) | the command index in `docs/src/cli/flags.md` (gated) + `skills/session-bootstrap.md` when the leaf carries a flag an agent must know about + a `docs/src/cli/` page when the leaf needs more than `--help` (both author judgement) | `TestSkillsCoverAllCliLeaves` (naming only — see the note at the end of this file) |
| A `--json` envelope or `format_version` value | CLAUDE.md "Output Format Contract" + payload schema `$id` version (`descriptor.PayloadSchemaFormatVersion`) | `TestClaudeMdMentionsFormatVersion`, `TestPayloadSchema_VersionMatchesEnvelope` |
| The public payload contract (any `types` request/response struct slot reachable from a payload; a schema-surfaced registry enum `types.All*Types` / `AllOverlayKinds` / `AllRegressionTypes`; or a strict-union shape `OverlayRef` / `OverlayPayload`) | regenerate `descriptor/testdata/payload-schema.json` (`go test ./descriptor/ -run TestPayloadSchemaGolden -update`) + `descriptor/schema.go` (only if enum/union/entry wiring changes) + `docs/src/contract/payload-schema.md`. New top-level operation payload also: add it as an entry in `BuildPayloadSchema`'s `entries`. | `TestPayloadSchemaGolden`, `TestPayloadSchema_EnumsMatchRegistry`, `TestPayloadSchema_VersionMatchesEnvelope`, `TestGoldensNotHandEdited` |
| A `.pulse` file format change (header, field type) | CLAUDE.md "Byte-layout invariants" + `skills/type-<kebab>.md` (atomic skill, one per `FieldType`) + `skills/cohort-schema-design.md` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllFieldTypes`, `TestClaudeMdMentionsFormatVersion` |
| A new non-skippable CI gate | CLAUDE.md "Non-Skippable CI Gates" list | `TestClaudeMdMentionsAllNonSkippableGates` |
| A new architectural decision | CLAUDE.md (relevant section) + PRD if applicable | reviewer enforcement |
| An environment variable | CLAUDE.md "Build / Env" + `skills/session-bootstrap.md` | `TestClaudeMdMentionsAllEnvVars` |
| A registered MCP tool (add/remove) | `skills/tool-<kebab>.md` (atomic skill; strip the `pulse_` prefix) + `mcp/toolmeta/meta.go` + `docs/src/internals/adding-mcp-tool.md` | `TestOperatorHasAtomicSkill`, `TestAtomicSkillHasRequiredSections`, `TestSkillTokenBudget`, `TestSkillsCoverAllMCPTools`, `TestManifestMCPToolsComplete` |
| Managed-import sidecar shape (`imports.Sidecar`) | `skills/session-bootstrap.md` + CLAUDE.md "Build / Env" | reviewer enforcement |
| A new MCP action tool with field-name params | `mcp/bind.go` + `docs/src/internals/adding-mcp-tool.md` | `TestMCPSchemaBinding_*` suite |
| The public MCP-serve entry (`mcpserve.Serve` / `mcpserve.ServeStdio`) | `mcpserve/` + `docs/src/cli/mcp.md` (Embedding section) | reviewer enforcement |
| The default operator table | CLAUDE.md "Smart defaults" + `skills/session-bootstrap.md` | `TestDefaults_Applied` + reviewer |
| A request example under `examples/` | `_meta` block (kebab name, category=dir, canonical tags, alphabetized operators matching body) | `TestExamples_*` suite |
| Example tag taxonomy | `CanonicalTags` in `examples/library.go` + `docs/src/examples/library.md` | `TestExamples_TagsFromTaxonomy` |
| `pulse.Options.Extensions` API or registration validation rule | `extensions_validate.go` + `docs/src/internals/extension-points.md` + CLAUDE.md "Extension Points" | `TestExtensions_*` suite |
| `FacetRequest` / `FacetResult` shape or facet streamability | `types/facet.go` + `skills/facet-design.md` + `descriptor/capabilities_facet.go` + `descriptor/facet.go` | `TestFacetSchema_*`, `TestManifestFacetCapability` |
| `FacetRequest.Overlays` slot, `FacetResult.Overlays` slot, the FACET-host overlay dispatch at `processing.ApplyOverlaysFacet`, or the FacetSchema-buffered-exit wiring at `service.applyFacetOverlays` | `types/facet.go` + `service/facet_overlay.go` + `service/facet_rich.go` + `descriptor/overlay_facet.go` + `descriptor/facet.go` + `descriptor/capabilities_facet.go` + `descriptor/overlay.go` + `skills/facet-design.md` + `skills/overlay-system.md` + CLAUDE.md Execution modes pointers | `TestFacetWithOverlays_*`, `TestValidateFacetOverlays_*`, `TestValidateOverlays_FacetKindOnRequest`, `TestSkillsCoverAllOverlayKinds`, `TestManifestFacetCapability` |
| `pulse.Options.ProjectBufferedFields` flag or `processing.NeededFields` extractor | CLAUDE.md "Byte-layout invariants" pointer + `docs/src/internals/extension-points.md` (FieldInputs hook) + `.claude/reference/execution-modes.md`. A NEW request slot that can carry an operator must be walked, and a NEW built-in whose `Params` name a schema field must gain an `addAggParamFields` arm — both fail SILENTLY, as a confident 0 with `n_null` equal to the full admitted count, and a unit test that hand-builds `Record` values cannot see either. Cover a new slot with a test that goes through the real decode path (see `service/crosstab_margin_agg_projection_test.go`) | `TestNeededFields_*`, `TestProjection_*`, `TestReadRecordProjected_*`, `TestCrosstabMarginAgg_ProjectionCoversAuxiliaryFields`, `TestProcess_ProjectionCoversDistinctByParam` |
| `FieldInputsFunc` hook on a registration struct | `extensions.go` + `extensions_runtime.go` + `docs/src/internals/extension-points.md` | `TestNeededFields_ExtensionWithFieldInputs`, `TestNeededFields_UnknownExtensionWidens` |
| `DecodePlan` segment shape, `Schema.BuildDecodePlan`, plan-driven `RecordReader.ReadRecordWithWidePlan`, or the iterator's per-iterator plan cache | `encoding/decode_plan.go` + `encoding/reader_plan.go` + `service/decode_plan_cache.go` + `skills/cohort-schema-design.md` + `docs/src/internals/extension-points.md` + CLAUDE.md "Byte-layout invariants" | `TestDecodePlan_Equivalence_*`, `TestCrosstab_DecodePlanEquivalence_*` |
| Whole-file slurp avoidance in `Service.Open` / streaming iterator init | `service/service.go` + `docs/src/internals/architecture.md` | `TestCountingFs_*` |
| `afero.Fs` eligibility probe (`service.RealPather`, wrapper-fs unwrap order) | `service/fs_probe.go` + `service/stream.go` + `skills/cohort-schema-design.md` + `docs/src/internals/adding-afero-fs.md` | `TestCountingFs_*` |
| Shard archive layout (entry names, `_schema.pulse` block, magic dispatch, dict prefix rule) | CLAUDE.md "Byte-layout invariants" + `skills/cohort-schema-design.md` + `docs/src/internals/managing-shard-archives.md` | `TestShardArchiveLayoutDocumented`, `TestSkillsCoverShardingTopics` |
| `ChainRequest` / `ChainResponse` shape OR `processing.CanChainRequest` gate | `types/chain.go` + `processing/chain.go` + `service/chain.go` + `descriptor/chain.go` + `descriptor/capabilities_chain.go` + `skills/process-chain.md` + `skills/session-bootstrap.md` | `TestProcessChain_*`, `TestValidateChain_*`, `TestSkillsCoverAllCliLeaves`, `TestSkillsCoverAllMCPTools` |
| `pulse.CountRecords` facade or its O(1) contract | `service/count.go` + `pulse.go` | `TestCountRecords_*` |
| The single-file RECORD-COUNT derivation (`encoding.Schema.RecordCountForPayload`) or either arm reading it — `service.CountRecords` and `descriptor.Inspect` (`deriveSingleFileRecordCount` → `InspectResult.RecordCount`) | `encoding/schema.go` (the one derivation) + `service/count.go` + `descriptor/inspect.go` + `docs/src/cli/cohort-inspect.md` + CLAUDE.md "Predict / Inspect contracts". **The arithmetic exists ONCE on purpose.** Two independent floor divisions over the same bytes is one edit away from two different record counts, and nothing on either wire says which arm produced the number a caller is holding. **The arms differ in OBSERVABILITY and only there, deliberately:** `Inspect` has an envelope and raises the `ENCODING_INVALID` truncated-tail warning naming the leftover bytes; `CountRecords` returns `(uint64, error)` with no warning channel and floors SILENTLY, because it feeds the parallel-decode eligibility gate as well as the facade and a half-written trailing record must not stop a cohort that still processes from reporting its whole-record count (measured: `api process` over such a cohort aggregates the floored 120 records and succeeds). Do not "fix" that asymmetry by making `CountRecords` error — change the derivation and both arms move together, which is the property the lift buys | `TestRecordCountForPayload_Matrix`, `TestCountRecords_TruncatedTailAgreesWithInspect`, `TestInspect_RecordCount*` |
| The `pulse cohort inspect` leaf's read path, `Pulse.InspectEnvelope`, or what `printInspectResult` renders | `internal/cli/cohort.go` + `pulse.go` (`Inspect` / `InspectEnvelope`) + `docs/src/cli/cohort-inspect.md` + `docs/src/library/overview.md` (public-method table) + CLAUDE.md "Design principles" (the facade method list). **Every mode reads through the facade.** The `--json` / `--full-dict` branch used to `os.ReadFile` the raw argument and hand the bytes to `descriptor.InspectFromBytes` itself, which bypassed the injected `afero.Fs` AND silently lost `archive.pulse#shard.pulse` anchor resolution — an anchor that inspected fine as text returned `data:null` plus an `INSPECT_ERROR` envelope. `InspectEnvelope` exists because `Inspect` can reach neither the `InspectOptions` knob nor `env.Warnings`; a leaf needing either must take the envelope sibling, never its own read | `TestCliCohortInspectJson_ResolvesShardAnchor`, `TestCliCohortInspectFullDict_ResolvesShardAnchor`, `TestCliCohortInspectText_ReportsRecordCount`, `TestCliCohortInspectFullDict_DisablesTruncation`, `TestInspectEnvelope_SurfacesWarningsThroughTheInjectedFS` |
| `mcp.InspectOut` — the `pulse_inspect` OUTPUT contract (the embedded `descriptor.InspectResult` plus the additive `warnings` slot) or `HandleInspect`'s read path | `mcp/contract.go` (`InspectOut`) + `mcp/handlers.go` (`HandleInspect` — reads `Pulse.InspectEnvelope`, never `Pulse.Inspect`, because the wrapper discards `env.Warnings`) + `mcp/toolmeta/meta.go` (`DescInspect`) + `descriptor/testdata/manifest.json` (golden regen — the manifest mirrors the tool description) + `skills/tool-inspect.md` + `docs/src/mcp/index.md` (tool table). **The result is EMBEDDED, so the slot is additive**: every key an agent already parses stays at the top level and a clean read emits no `warnings` key at all, which is what keeps the wire form byte-identical for the ordinary case. The slot exists because `record_count` is DERIVED and a torn payload tail reports the FLOOR with the `ENCODING_INVALID` warning as its only signal — the CLI reads that warning through `InspectEnvelope`, and a tool routed through `Pulse.Inspect` cannot see it at all. Warnings are `[]*descriptor.EnvelopeEntry`, not `[]string`, so the code is usable with `pulse_errors_lookup` and `details` carries `record_stride` + `trailing_bytes`. `format_version` does NOT move — `descriptor.InspectResult` is not payload-reachable and `payload-schema.json` is untouched | `TestHandleInspect_SurfacesTheTruncatedTailWarning`, `TestInspectSchema_DeclaresTheWarningsSlot`, `TestManifestMCPToolsComplete`, `TestGoldensNotHandEdited`, `TestSkillsCoverAllMCPTools` |
| `Request.Joins` slot or `JoinSpec`/`OnPair` shape | `types/join.go` + `processing/join.go` + `service/join.go` + `descriptor/join.go` + `descriptor/capabilities_join.go` + `skills/join-design.md` | `TestJoin_*`, `TestValidateJoin_*` |
| Cohort-analytics aggregator catalog (`AGG_WEIGHTED_MEAN`/`RATIO`/`CI_LOWER`/`CI_UPPER`) | `processing/aggregator_cohort.go` + `types/types.go` + `types/streamability.go` + `descriptor/capabilities_aggregators.go` + `skills/aggregation-design.md` | `TestAggregator_*`, `TestManifestOperatorsComplete`, `TestStreamability_AggregationsKnown` |
| `ExportJob.Includes` / `ConvertJob.Includes` projection, `IncludeOverlays` opt-out, `Hash` canonical surface, or its CLI `--include` / `--include-overlays` flags | `io/io.go` + `io/hash.go` + `io/export.go` + `io/convert.go` + `internal/cli/export.go` + `errors/codes.go` + `errors/fixup_metadata.go` + `docs/src/internals/adding-io-format.md` | `TestExportJob_*`, `TestConvertJob_*`, `TestCodesHaveFixups` |
| `pulse.Options.Extensions.LabelTables`, `types.LabelBinding` on requests / jobs, `PULSE_LABEL_TABLES_DIR` loader | `extensions.go` + `extensions_validate.go` + `extensions_runtime.go` + `extensions_snapshot.go` + `processing/extensions.go` + `processing/labels.go` + `types/labels.go` + `descriptor/labels.go` + `descriptor/extensions.go` + `io/labels.go` + `io/io.go` + `io/export.go` + `io/convert.go` + `label_adapter.go` + `label_loader.go` + `internal/cli/labels.go` + `mcp/bind.go` + `skills/label-display.md` + CLAUDE.md "Build / Env" | `TestExtensions_LabelTable*`, `TestLabelResolver_*`, `TestValidateLabels_*`, `TestSampleWithRequest_*`, `TestProcess_GroupKey_*`, `TestProcess_Labels_*`, `TestFacetSchema_Labels_*`, `TestExportJob_Labels_*`, `TestLoadLabelTables_*`, `TestParseLabelBindings_*`, `TestMCPSchemaBinding_Labels*`, `TestSkillsList_ReturnsAll`, `TestSkillsNames`, `TestClaudeMdMentionsAllEnvVars` |
| The synth PROFILE DOCUMENT shape — a `synth.Profile` / `ConditionalProfile` section, `synth.FieldModel` / `ModelPredictor` / `ModelReference`, a `ProfileOptions` capture flag, or the `SpecFromProfile` translation rule that picks between two sections | `synth/profile.go` + `synth/profile_models.go` + `synth/spec.go` (the `*Spec` counterpart slot) + `internal/cli/synth.go` (the `profile create` flag) + `skills/synthetic-data.md` (Profile mode) + `docs/src/cli/profile-create.md` + CLAUDE.md "Synthetic data". **Sections grow ADDITIVELY only** — new keys are `omitempty`, an absent section reproduces today's behaviour exactly, and a document written before the section existed stays valid input forever; assert that with a checked-in pre-change fixture rather than by re-capturing. Decide EXPLICITLY which parts of a shape are durable: an internal encoding (a design-column name) and a bounded row-aligned reservoir (fitted residuals) are `json:"-"` on purpose, and a consumer that needs them must hold the capture rather than re-read the file. `format_version` does NOT move — `synth.Profile` / `synth.Spec` live in `synth/`, not `types/`, so they are unreachable from `descriptor.BuildPayloadSchema`; verify that rather than assume it | `TestProfile_ModelsSection_IsPurelyAdditive`, `TestProfile_ModelsSection_RoundTripFullFidelity`, `TestSpecFromProfile_PreModelsDocumentUnchanged`, `TestSpecFromProfile_ModelsRetireNumericTargetArms`, `TestPayloadSchemaGolden`, `TestGoldensNotHandEdited` |
| The synth FIDELITY REPORT shape — a `synth.FidelityReport` section, one of its per-entry structs, the recovery SCALE a section compares on (`synth/fidelity_score.go`), or the rule deciding WHICH relationships a section scores | `synth/fidelity.go` (the report struct) + the section's own file (`synth/fidelity_models.go` for `models`, `synth/fidelity_score.go` for the recovery scale) + `synth_fidelity.go` (the facade bridge that assembles and writes the document) + `skills/synthetic-data.md` (Fidelity report) + `docs/src/cli/synth-from-profile.md` + CLAUDE.md "Synthetic data". **Every section scores ONLY relationships generation ACTUALLY APPLIED** — this is the v0.32.2 bug class and it recurs easily: a section built off `Spec`'s own unpruned slices reports a computed-looking delta for a relationship the generator never ran, and the number looks like evidence. Re-run generation's OWN arbitration (`synth.ResolveConflicts` for the six pairwise lists; `resolveConflicts` + `buildModelDrawers` for `models`, which adds two warn-and-skip refusals of its own) against the SAME `*Spec` `generate()` was given; both are pure functions of the Spec, so the reproduction is exact. Sections are `omitempty` and additive — a report for a spec carrying nothing of that kind stays byte-identical. **Reuse `decodeSyntheticRows`**: v0.32.1 fixed an O(pairs × records) blowup by decoding the synthetic partition once, and a new section with its own full decode per entry reintroduces it in a new place. **A comparison whose left-hand side is a NON-INVERTIBLE transform of the generated value must be calibrated on BOTH sides or refused outright.** A `bernoulli` / `discrete` target has a step/staircase `Q` with no point latent inverse, and the obvious remedy — an interval-midpoint probit score — is systematically ATTENUATED, i.e. it reports a wrong number for a generation path that is exactly correct, which is worse than reporting nothing. `synth/fidelity_score.go` closes it by projecting the SAME score twice, once from the generated values and once as the conditional mean the CAPTURED model implies per row; OLS is linear in its response, so `E[b_observed | X] == b_expected` exactly and the ratio `b_expected / captured` (shipped as `score_retention`, and applied to the standard error too) removes the attenuation rather than estimating it. That is why there is **no K gate** (K = 2 is merely the lowest-retention case, not a degenerate one) and **no ordered-probit refit** (it would be more efficient, not more correct). The same trick does NOT extend to a CORRELATION — the factor depends on the pair's joint distribution, not on either marginal — so a staircase endpoint stays `no_model_fit` in `model_residual_correlations`, counted rather than silently attenuated. A new section reporting a derived quantity must say which of those two shapes it is. **A section whose entry count is QUADRATIC in fields must be BOUNDED at the source** — the report is already 1.65 MB and ~2 min on the motivating cohort, and 55 applied models is C(55,2) = 1,485 pairs; carry the aggregate counters over EVERY entry, list the worst N (`maxResidualRecoveryPairs`, matching `maxThinLevelWarnings` / `maxThinResidualPairWarnings` at 20) worst-first, and COUNT the remainder rather than dropping it. A section nobody can read is not an improvement over a section that lied. **Name a new section for its MECHANISM, not its statistic**: `pairwise` (value-scale copula) and `model_residual_correlations` (residual-scale correlator) are different numbers over the same two fields, and the section name is the only thing telling a reader scanning the JSON which one they hold. **Two sections describing rival mechanisms must be DISJOINT and it must be structural** — `models` versus `categorical_numeric_pairwise` / `set_numeric_pairwise` is guarded twice (`SpecFromProfile` omits a modelled target's pair; `resolveConflicts` pre-claims the field), and the retirement is per TARGET, never per document: a captured-but-unapplied model must leave its field's pair standing, or the field is scored nowhere and reconstructed from nothing. `format_version` does NOT move — `synth.FidelityReport` lives in `synth/`, not `types/` | `TestBuildModelFidelity_*`, `TestBuildModelResidualFidelity_*`, `TestFidelityReport_NumericTargetSectionsRetiredForModelledTargets`, `TestFidelityReport_OldFormatSpecCarriesNoNewKeys`, `TestBuildPairwise_*`, `TestSynth_FidelityReport*`, `TestLatentFor_InvertsQuantileForEveryDistribution`, `TestLatentFor_EveryDistributionIsClassified`, `TestPayloadSchemaGolden` |
| The synth GENERATION-SIDE model contract — `Spec.Models` / `Spec.ResidualCorrelations`, the `drawRow` composed draw `value = Q(Φ(μ + σ·z))`, the drawer-compilation rules in `buildModelDrawers` / `buildResidualCorrelator`, or the `resolveConflicts` claim-space priority order | `synth/spec.go` (the slots) + `synth/model_draw.go` (the draw) + `synth/residual_draw.go` (the shared `z`) + `synth/conflict.go` (the claim order) + `synth/copula.go` (`quantileFor`, its inverse `latentFor`, AND `synth/fidelity_score.go`'s `isStaircaseDistribution` — THREE switches that move TOGETHER or a distribution silently drops out of the fidelity section; `TestLatentFor_EveryDistributionIsClassified` is the forcing gate and asserts all three per arm of its refused / round-trip / step-quantile partition) + `skills/synthetic-data.md` (Multi-predictor models) + `docs/src/cli/synth-from-profile.md` + CLAUDE.md "Synthetic data". **Effects are LATENT-scale**: a coefficient shifts `μ` on the standard-normal scale, so it is non-linear in value space for every non-normal `Q` and must never be surfaced as "this many units of the field" — restate that wherever a coefficient becomes visible (document, fidelity entry, docs page), because it is the property a reader gets wrong silently. **The value-space alternative (draw the marginal, then add the prediction) is REJECTED, permanently** — it destroys the fitted marginal it was meant to protect. **One clamp, on the final data-scale value**, model bounds winning over the marginal's, so the model stage and the copula stage cannot disagree about the admissible range. **Determinism is the ordering contract**: drawers sort into SCHEMA FIELD ORDER (never `models` array order, never map order), every drawer consumes exactly one normal per row UNCONDITIONALLY (even when `residual_std` is 0), the correlated vector consumes one normal per participant in component order before the first drawer runs, and fewer than two participants compiles no correlator at all — so a spec declaring none is byte-identical to pre-slot output. Any new stage in `drawRow` must state where it sits relative to the model stage, which runs LAST because its predictors are values earlier stages can still rewrite | `TestSynthModel_*`, `TestSynthResidual_*`, `TestSynthModel_ShapeFittedTargetKeepsShapeAndGainsConditioning`, `TestLatentFor_InvertsQuantileForEveryDistribution`, `TestModelPredictorKind_CoversEveryDummyColumnKind`, `TestSpecFromProfile_ModelsRetireNumericTargetArms` |
| A synth type-NAME PREDICATE — `isNumericFieldType` (copula participation), `isBooleanFieldType` (the `bernoulli` marginal arm), `isIntegerQuantizedFieldType` (the `discrete` marginal arm) or `sentinelFor` (the expr environment's Go shape) — or a new field type in `fieldTypeFromName` | `synth/copula.go` + `synth/discrete.go` + `synth/constraints.go` + `synth/writer.go` (`fieldTypeFromName`) + `skills/synthetic-data.md` (Pairwise correlations participants / the marginal arms) + `docs/src/cli/synth-from-profile.md` (the per-type reconstruction list). **DERIVE the answer from `fieldTypeFromName`, never transcribe a second list of type-name strings.** Both defects this rule exists for were transcription: `sentinelFor` typed `packed_bool` as a Go `bool` while the sampler emits `float64` and broke every constraint over 90 of a survey cohort's 122 fields (issue #258), and `isNumericFieldType` listed three `nullable_*` spellings no `Spec` can declare while omitting `u4`, which dropped ALL 16 of that cohort's captured numeric pairs and 11 of its 14 integer columns out of `Spec.Correlations` in silence. **The boundary for copula participation is TYPE, not distribution** — every declarable scalar qualifies (`u4`…`u64`, `f32`/`f64`, `date`, `decimal128`, `packed_bool`; `categorical_*` and `set_*` do not, because their row value is not a number the transform could write back) and a field whose distribution has no moments is refused one call later by `fieldMoments`, naming the distribution. **A predicate must exclude `packed_bool` / `decimal128` only with a reason that survives the profiler**: both land in `ProfileBytes`' NUMERIC accumulator, so both can appear in `conditional.numeric_pairs`, and excluding one reproduces the same silent drop one type over. Gate the whole set with an EXHAUSTIVE table driven off the type enum, never a hand-picked sample — the name that is missing is by definition not in a sample | `TestFieldTypePredicates_ClassifyEveryDeclarableType`, `TestFieldTypePredicates_RefuseUndeclarableNames`, `TestSentinelFor_MatchesDrawnRowValueForEveryDeclarableType`, `TestSpecFromProfile_SmallIntegerPairsReachTheCorrelationMatrix`, `TestSynthCorrelation_DiscreteMarginsHoldAndCorrelate`, `TestProfileDiscrete_EveryIntegerTypeClaimsTheArm` |
| The synth STRUCTURAL-RULE contract — `Spec.Rules` / `RuleSpec`'s five slots (`when` / `set_null` / `set` / `set_expr` / `null_together`), its one MODIFIER (`owns_nulls`), the eager `validateRules` refusal set, or the `PULSE_SYNTH_RULE_*` family | `synth/rules.go` (the document model + validation) + `synth/rules_apply.go` (`compileRules` + the per-row `ruleApplier.apply`) + `synth/rules_ownership.go` (`owns_nulls`: `ruleOwnedNullFields` + `ruleOwnedNullSampler` + `buildOwnedNullFields` + `ownershipWarnings` + `nullOwnershipDivergenceThreshold` / `ownershipNoiseStdErrs`) + `synth/rules_apply.go` (`applyNullTogether` + `nullTogetherWarnings` + `nullRateDivergenceThreshold`) + `synth/rules_coerce.go` (THE COERCION MATRIX — `ruleValueFault`, shared by the `set` literal path and the `set_expr` result path) + `synth/writer.go` (`rowStages.rules`, compiled in `generate`, run as `drawRow`'s LAST act; plus `buildSchema`'s owned-null sampler WRAP and `generate`'s `commitOwnedNulls` / `ownershipWarnings` calls) + `synth/spec.go` (the slot) + `synth/constraints.go` (`rowExprEnv` / `rowExprOptions` — the ONE env builder `when` / `set_expr` and `Spec.Constraints` share) + `errors/codes.go` + `errors/fixup_metadata.go` (`DetailSynthRule` / `DetailSynthRuleSlot`, Message + ≥1 Fixup each) + `descriptor/testdata/manifest.json` (golden regen for the codes) + `skills/synth-structural-rules.md` + `docs/src/cli/synth-from-schema.md` + CLAUDE.md "Synthetic data". **`set` and `set_expr` stay SIBLING KEYS** — one map with a `{"$expr": …}` marker leaves `{"set": {"region": "west"}}` undecidable between a categorical literal and a bare identifier, and the two-key shape makes the question impossible to ask; this was settled in the interview, do not revisit. **Declaration order, sequentially, last write wins, one pass LAST** (after the model stage, so the semantics are `if gate then null else inferred` and a gated field keeps the value its model produced on every non-gated row). **The placement is the design, not an optimisation** — moving the pass ahead of `stages.models` hands the model the last word on its own target, and the accepted consequence of running last is that a field a rule NULLS still contributed its DRAWN value to any model that used it as a PREDICTOR on that row; the two-phase alternative was rejected in the interview and must not be reintroduced as a bug fix. `set_null` sets the mask and LEAVES the drawn value in the row (matching `nullableSampler`, so `isnull()` reads alike whatever produced the null); `set` writes the value and CLEARS the mask, or a drawn null would write 0 and the literal would silently vanish. A `set` literal is coerced to the row's Go shape ONCE at compile time through `constantRowValue` — never a second conversion, that is how a `set_*` target's `[]any` becomes the row's `map[string]bool`. **`set_expr` writes and clears the mask exactly as `set` does, and goes through the SAME matrix**: `ruleValueFault` answers for a literal and for a computed value alike, and a second type/range/domain table is the shape E1-S6 exists because of — a disagreement here is SILENT (a `u4` taking 16 is masked to its low nibble and stored as a real-looking 0). The matrix by target class: a scalar takes a bool as 1/0 or a range-checked number and refuses a string; a `categorical_*` takes a string inside the field's declared domain; a `set_*` takes a selection (`map[string]bool`, an option list, `split()`'s `[]string`). **The ONE asymmetry is `decimal128` from a string — a `set` LITERAL keeps it verbatim and exact, a `set_expr` result is REFUSED** (expr already reduced the value to `float64` so the exactness is fiction, and a string in the row for a field `rowExprEnv` typed `float64` breaks the NEXT rule reading it, intermittently and only on gated rows); do not "fix" the asymmetry by accepting it. **TWO TIMINGS, ONE CODE**: a fault the RETURN TYPE settles is refused at SPEC PARSE (`validateRuleExpr` off `prog.Node().Type()`; an `interface{}` type defers whole), a fault only a VALUE settles at ROW time, both `PULSE_SYNTH_RULE_VALUE_INVALID` with the slot in details — and the static side must keep PROBING the runtime matrix with the type's zero rather than growing a table of its own, or the two timings can disagree. **INTRA-RULE ORDER IS UNOBSERVABLE BY DESIGN**: every expression in one rule, `when` included, reads the row as it stood BEFORE the rule ran and all the rule's writes land after (SQL-`UPDATE` snapshot semantics, so `{"a": "b", "b": "a"}` swaps). Do not "simplify" it into evaluate-and-assign per key — the order that exposes is ALPHABETICAL, unreadable off a JSON document, and renaming a column would change a number; and do not refuse intra-rule reads instead, which also refuses `{"nps": "nps + 1"}`. ACROSS rules an expression still sees EARLIER writes and not LATER ones. **A NULL THE FILE CANNOT RECORD IS REFUSED FOR `set_null` AND WARNED FOR `null_together`, AND THE ASYMMETRY IS THE RULE RATHER THAN AN OVERSIGHT** (`encodeRow` writes the bitmap for nullable fields only, so the value goes out as the type's zero and a `u4`/`packed_bool` 0 is indistinguishable from a real one). The refusal is keyed on WHAT THE SLOT CLAIMS: `set_null` states the field IS null on a matching row, which a non-nullable field can never honour, so it is `PULSE_SYNTH_RULE_FIELD_NOT_NULLABLE` at spec parse (`ruleSetNullNullableFault`, reported LAST within a rule so a broken `when` or an unwritable `set` literal is named first); `null_together` states the members carry the GATE's decision, which includes UN-nulling and may be "present" on every row, so it stays a warning. **Do NOT "finish the job" by promoting `null_together` as well** — the gated-block idiom names a NEVER-NULL field first on purpose (the copy clears every member's own MCAR null and a later `set_null` becomes the block's only absence), so a refusal keyed on "any field a rule may null" refuses the most useful shape the layer has; the falsification exists (`TestRules_GatedBlockIdiomValidatesAndRaisesNoNullabilityWarning`) and a careless promotion also breaks two E1 tests. The warning kind lives in `synth/warning_summary.go` beside the string builder, per that file's own rule. **An undeclared field name inside `isnull("…")` is also refused at spec parse**, as `PULSE_SYNTH_RULE_FIELD_UNKNOWN` and deliberately NOT `_EXPR_INVALID`: the expression COMPILES (the builtin's signature is `func(string) bool`), so it is an unknown FIELD and widening "does not compile" to cover it would make the code mean two things. The detector (`isnullUnknownField`, `synth/constraints.go`) PARSES the author's source and walks the AST — never a substring probe, for the same reason `exprReadsIdentifier` does: a field named `isnull_reason`, and the text `isnull("x")` sitting inside a string LITERAL, must not trip it. It is applied to `compileConstraints` as well as to `validateRules`, because the two surfaces share one environment and `TestValidateRules_ExprEnvIsTheConstraintEnv` holds their verdicts equal. **A `when` that fails at RUN time keeps `PROCESSING_RUNTIME` and must NOT acquire a `PULSE_SYNTH_RULE_*` code of its own** — the remaining causes are generic expr evaluation faults (an `isnull` argument that is only a string at run time, `int()` of a non-numeric string), identical in kind to a constraint's own evaluation failure, and the coded error already carries `DetailSynthRule` + the slot + the expression; the path is still REACHABLE (`TestRules_WhenRuntimeFaultIsStillReachable`) so the backstop is not dead code, and a code minted for it would either duplicate `_EXPR_INVALID`'s prose or describe a fault nobody can act on differently. **`null_together` COPIES `nullMask[block[0]]` ONTO EVERY OTHER MEMBER, and the copy goes BOTH ways.** A gate that is null nulls the block; a gate that carries a value UN-NULLS every member that drew a null of its own, and dropping that second direction is the silent half — the block still looks co-missing and the realized all-present rate stays the PRODUCT of the members' rates, which is the whole defect (45 of 40,000 rows against ~6,960 on the motivating profile, `0.174⁴` not `0.174`). Copying is forced, not chosen: every field has consumed its own null draw before the pass runs and the pass consumes NO RNG, so there is no other resolution available. **Consequence, which must be stated wherever the slot is described: every member but the first has its own `null_rate` IGNORED**, reported by `nullTogetherWarnings` past `nullRateDivergenceThreshold` (**0.02**, a package constant with its reasoning at the declaration — do not promote it to an `Options` field; a knob here would mostly be used to silence the one warning that stops a declared rate vanishing unannounced). The comparison is ABSOLUTE and per-member against the GATE, never the block's max-minus-min: what the copy discards is a count of ROWS. Refusal was considered and REJECTED — a block whose fields drifted slightly must stay usable. The warning is an ATTENTION kind in `synth/warning_summary.go`; a non-nullable member reuses the silent-zero kind, so that matcher must stay slot-AGNOSTIC (`"rule " … " names non-nullable field "`) — `null_together` is its only emitter now that `set_null` refuses, and a slot-specific matcher would drop it into the catch-all. **Both of `nullTogetherWarnings`' loops skip `block[0]`**: the divergence loop because it measures AGAINST the gate, the nullability loop because `applyNullTogether` never WRITES the gate's mask, so reporting it was accurate about the declaration and wrong about the consequence — and noisy for exactly the idiom above (measured on the motivating profile: one spurious line, 797 warnings in 4 kinds down to 796 in 3, generated bytes unmoved). **WITHIN one rule the block is the LAST write**, after `set_null` / `set` / `set_expr`: the slots of a rule have no author-readable order, and last is the composition that does what it reads like (`set_null` the gate, the block follows). Reversed, the block would be decided from the gate's DRAWN state and the `set_null` would break it apart on exactly the rows it was meant to gate — so a `set_null` naming a NON-gate member of the same rule is overridden by the block, deliberately. ACROSS rules the ordinary last-write-wins applies instead. The block moves the null DECISION only and never a value. **Never topologically sort** — that makes the applied order implicit, and declaration order is the one ordering an author can read off the document. **`owns_nulls` is a MODIFIER on `set_null`, not an action, and it ZEROES the field's own null draw rather than applying the residual `(r - g) / (1 - g)`** — the residual needs `g`, the rule's FIRING PROBABILITY, which is a property of the data and not of the spec (a `when` is an arbitrary predicate, and estimating `g` would need a generation pass whose own rows are drawn from the rates being corrected). So the gap is MEASURED at generation and reported (`ownershipWarnings`), never guessed: `nullOwnershipDivergenceThreshold` (0.02) AND `ownershipNoiseStdErrs` (2) must BOTH be exceeded, and dropping the noise term makes the warning a coin toss on small runs (at 200 rows a correct claim on a 0.25 field misses by 0.02 about half the time). It is its own constant rather than `nullRateDivergenceThreshold` because the two bound different mechanisms — a COPY from a sibling, knowable at compile time, versus a GATE's firing rate, knowable only once rows exist. **Suppression is a WRAPPER that discards the verdict (`ruleOwnedNullSampler`), never a rebuild without the nullable wrapper**: the owned field must consume the IDENTICAL draws, or the seeded stream shifts and ownership stops being diffable against an un-owned run (measured: 72 other fields cell-identical, 381,351 null flags removed, 0 added, 0 values moved). **Ownership is a UNION, never a `resolveConflicts` claim** — `set_null` removes a value rather than supplying one and has never claimed a field away from its own generation, so two rules may own one field and the draw is suppressed once. `owns_nulls` with an EMPTY `set_null` is `PULSE_SYNTH_RULE_OWNERSHIP_INVALID`, because an ignored claim is invisible. **`null_together` already owns its non-gate members, by COPY**, so the gated-block idiom is ownership as a side effect; the block stays required for a co-missing block with no gate at all. **Validation is EAGER or it is worthless**: a rule naming a mistyped field or carrying an uncompilable expression is INVISIBLE at run time (the run succeeds, the gate is simply absent), so a new fault class belongs in `validateRules` with its own code and a refusal at spec parse, never a row-time check. Faults walk a FIXED slot order with SORTED map keys, so a rule with two faults refuses the same one on every run — Go map order in a validator makes the error an author sees depend on the runtime's seed. **The standalone rules-file format is the `Spec.Rules` array itself**, so an inline declaration and a `--rules` file stay the same JSON and the loader needs no translation layer; a wrapper object would be two formats to learn. Every error carries the rule INDEX (`errors.DetailSynthRule`), the only handle back to a document whose rules have no names. The rule pass consumes NO RNG, so a spec declaring no rules must stay byte-identical to pre-slot output — assert it, do not assume it. `format_version` does NOT move — `synth.Spec` lives in `synth/`, not `types/` | `TestValidateRules_Matrix`, `TestValidateRules_ErrorNamesRuleIndexAndField`, `TestValidateRules_FaultChoiceIsDeterministic`, `TestValidateRules_ExprEnvIsTheConstraintEnv`, `TestRules_StandaloneFileShapeIsTheSpecSlot`, `TestRules_AbsentSlotRoundTripsByteIdentically`, `TestRules_ValidateAndApply`, `TestRules_ParseSpecRefusesEagerly`, `TestRules_GateMasksAndReplacesWhileTheInferredValueSurvives`, `TestRules_RulesFreeSpecIsByteIdenticalToPreStory`, `TestRules_ConsumeNoRNG`, `TestRules_DeclarationOrderLastWriteWins`, `TestRules_AppliedOnlyToTheGeneratedPartition`, `TestRules_GateComposesWithTheFieldsOwnNullRate`, `TestRules_NullTogetherSharesOneDecision`, `TestRules_NullTogetherCopiesTheFirstFieldsRateAndSaysSo`, `TestRules_NullTogetherAgreeingRatesAreSilent`, `TestRules_NullTogetherAndSetNullComposeByDeclarationOrder`, `TestRules_NullTogetherIsTheRulesLastWord`, `TestRules_NullTogetherConsumesNoRNG`, `TestRules_NullTogetherIsLive`, `TestRules_NullTogetherRespectsItsGate`, `TestNullTogetherWarnings_DivergenceBoundary`, `TestNullTogetherWarnings_DivergenceIsMeasuredAgainstTheGate`, `TestNullTogetherWarnings_NonNullableMemberIsReportedPerField`, `TestNullTogetherWarnings_GateIsNotReported`, `TestApplyNullTogether_CopiesTheGateBothWays`, `TestValidateRules_SetNullNonNullableIsRefused`, `TestRules_SetNullOnNonNullableFieldIsRefused`, `TestRules_GatedBlockIdiomValidatesAndRaisesNoNullabilityWarning`, `TestRules_NullTogetherStillWarnsForANonGateMember`, `TestIsnullUnknownField_ParsesRatherThanMatchesText`, `TestValidateRules_IsnullStringFormRefusedAtParse`, `TestValidateRules_IsnullDynamicArgumentIsStillDeferred`, `TestConstraints_IsnullStringFormRefusedBeforeAnyRow`, `TestRules_WhenRuntimeFaultIsStillReachable`, `TestRuleCoercion_MatrixCoversEveryDeclarableTargetType`, `TestRuleCoercion_BoolIsOneOrZeroOnEveryScalarTarget`, `TestRuleCoercion_NumberObeysTheSameRangeRulesAsALiteral`, `TestRuleCoercion_StringMustBeInTheTargetsDeclaredDomain`, `TestRuleCoercion_DecimalStringIsRefusedFromAnExpression`, `TestRuleCoercion_StaticRefusalAgreesWithTheRuntimeMatrix`, `TestRuleCoercion_UnknowableReturnTypeDefersToRunTime`, `TestRules_SetExprThreeBandNPS`, `TestRules_SetExprSnapshotMakesIntraRuleOrderUnobservable`, `TestRules_SetExprReadsEarlierRuleButNotLater`, `TestRules_SetExprCoercesOnTheWireForEveryTargetClass`, `TestRules_SetExprStringOutsideTheDeclaredDomainIsRefused`, `TestRules_SetExprTimingSplit`, `TestRules_SetExprConsumesNoRNG`, `TestOwnsNulls_KeepsTheGateExactAndTheMarginalCaptured`, `TestOwnsNulls_MovesTheNullMaskAndNothingElse`, `TestOwnsNulls_AbsentFlagIsTheUnchangedPath`, `TestOwnsNulls_RefusedWithNoSetNull`, `TestOwnsNulls_ReportsAClaimTheRunDidNotBearOut`, `TestOwnsNulls_TwoRulesMayOwnOneField`, `TestOwnsNulls_SilentOnSamplingNoise`, `TestCodesHaveFixups`, `TestManifestErrorCodesComplete`, `TestGoldensNotHandEdited` |
| The structural-rule PRE-CLAIM — which `Spec.Rules` shapes take a field away from its linear model, conditional pairs and residual correlations, and where the claim sits in `resolveConflicts`' priority order | `synth/rules_claim.go` (`ruleClaims` + `exprReadsIdentifier` — the keying rule and its four exclusions) + `synth/conflict.go` (the priority-0 loop) + `skills/synth-structural-rules.md` (What a rule retires) + `skills/synthetic-data.md` (the priority-order block — one cross-reference line, no per-rule detail) + `docs/src/cli/synth-from-profile.md` (What a rule retires from the derived spec, AND the `models` fidelity section — the claim changes which entries EXIST) + CLAUDE.md "Synthetic data". **The claim sits at PRIORITY 0, ahead of the model pre-claim**, because the rule pass runs last in `drawRow` and an unconditional write wins outright. **The reason is the FIDELITY REPORT, not the wasted work**: `BuildModelFidelity` asks generation's own compiler which models ran (`resolveConflicts` → `buildModelDrawers`), so without the claim a rule-overwritten modelled field ships a captured-versus-recovered coefficient delta for a value nothing kept — the same silent-inertness class as v0.32.2 and the E2-S5 predictor-kind default. Assert the disappearance ON THE REPORT, never by inferring it from a warning. **Four exclusions, each SILENT if got backwards.** A rule carrying `when` writes only some rows, so claiming strips the model from every row it never touches and leaves a plausible bare marginal. **`set_null` NEVER claims, at any conditionality** — it removes a value rather than supplying one, and `if gate then null else inferred` needs the field's own model to produce what the non-gated rows keep; this is the criterion to assert end to end on generated bytes. `null_together` supplies no value either. **A `set_expr` that READS ITS OWN TARGET does not claim** — it transforms what generation produced rather than determining it, and the canonical instance is the documented pre-rounding remedy `{"when": "!isnull(nps)", "set_expr": {"nps": "round(nps)"}}`, where a claim would leave the rounding applied to a bare marginal draw (measured: it strips `nps`'s model and all 54 of its residual correlations on the motivating profile). Detect the reference on the PARSED expression, never by substring (`nps_reason` is not `nps`); an unparseable expression answers "reads", the direction that cannot lose structure. The no-`when` restriction is sufficient rather than limiting because `set_expr` collapses the motivating three-band case into one unconditional rule. **Reuse the existing wording, do not invent a second phrasing**: the model and any conditional pair lose through the ordinary `conflict()` (`conditional relationship conflict: … is already claimed by structural rule N (set_expr); dropping …`), and the residual-correlation drop rides `buildResidualCorrelator`'s existing dropped-participant warning. **One contract NARROWS**: the pass still consumes no RNG, but a claiming rule retires a model and a retired model stops consuming its own unconditional per-row normal, so the stream legitimately moves — restate that wherever "a spec with rules draws the identical sequence" was written. A spec with no rules, and a spec whose rules claim nothing, must both stay byte-identical | `TestRuleClaim_UnconditionalValueRuleRetiresTheModel`, `TestRuleClaim_ExcludedModelDisappearsFromTheFidelityReport`, `TestRuleClaim_ExcludedFieldLeavesTheResidualCorrelation`, `TestRuleClaim_ConditionalRuleDoesNotClaim`, `TestRuleClaim_SetNullNeverClaims`, `TestRuleClaim_NullTogetherDoesNotClaim`, `TestRuleClaim_SelfReferentialSetExprDoesNotClaim`, `TestRuleClaim_ClaimedFieldDropsAConditionalPair`, `TestRuleClaim_ClaimingRuleShiftsTheStreamNonClaimingDoesNot`, `TestRuleClaims_KeyingAndOrder`, `TestExprReadsIdentifier_ParsesRatherThanMatchesSubstrings`, `TestRules_ConsumeNoRNG`, `TestRules_RulesFreeSpecIsByteIdenticalToPreStory` |
| The profile-path rule/spec plumbing — `synth.WriteSpec` (`--emit-spec`), `synth.ApplyRulesFile` / `synth.ParseRules` (`--rules`), or their wiring on `pulse synth from-profile` | `synth/spec_io.go` + `internal/cli/synth.go` (flags + `cliCodedError`) + `skills/synth-structural-rules.md` (Reaching rules from a profile) + `skills/session-bootstrap.md` (Synth-generation CLI flags) + `docs/src/cli/synth-from-profile.md` + `docs/src/cli/flags.md` (flag section + command index) + CLAUDE.md "Synthetic data". **`--emit-spec` must write the spec that GENERATED**, not a rendering and not a re-derivation, and the only assertion that proves it is a BYTE comparison of two cohorts generated at one seed from the derived spec and from `ParseSpec(emitted)` — a test that asserts the JSON parses passes against an emit that dropped `models`, coarsened a float, or re-derived the document, all three silent and all three previously shipped by this package. The round-trip fixture must carry a `models` section: the two checked-in profile documents predate it, and a fixture without one stops testing coefficients while still looking like coverage. **`--rules` REPLACES `Spec.Rules`** — a derived spec carries none, so an append mode would only invent an ordering question; `[]` means "no rules" and a `{"rules": […]}` wrapper is REFUSED rather than read as zero rules. Validation runs at the LOAD against a COPY (a profile-derived spec bypasses `validateSpec` by design, so there is no earlier gate, and a refused file must leave the slot untouched), keeps E1's own `PULSE_SYNTH_RULE_*` code, and adds `details["path"]` — an analyst holding a rules file, a spec and a profile cannot act on a rule index alone. The load, the merge and the emit live in `synth/`; the CLI supplies paths only. Emit runs BEFORE generation, so a failing run still leaves the document. Both flags absent must be byte-identical and the `--json` envelope must not move; `format_version` stays `"1.1"` | `TestEmitSpec_RoundTripsToByteIdenticalCohort`, `TestWriteSpec_CarriesMergedRules`, `TestWriteSpec_IsIndentedAndCarriesEverySlot`, `TestApplyRulesFile_LoadsMergesAndFires`, `TestApplyRulesFile_ReplacesRatherThanAppends`, `TestApplyRulesFile_RefusalsNameThePath`, `TestApplyRulesFile_MissingFileNamesThePath`, `TestApplyRulesFile_EmptyArrayIsAcceptedAndInert`, `TestParseSpec_SetConstantAcceptsTheJSONObjectForm`, `TestSynthFromProfileCLI_EmitSpecRoundTripsThroughFromSchema`, `TestSynthFromProfileCLI_EmitSpecDoesNotChangeTheCohort`, `TestSynthFromProfileCLI_RulesFileAppliesToGeneratedRows`, `TestSynthFromProfileCLI_RulesRefusalNamesTheFile`, `TestSynthFromProfileCLI_EmitSpecWithJSONKeepsTheEnvelope` |
| The structural-rule DETECTION surface — `ProfileOptions.SuggestRules` / `Profile.RuleCandidates`, the gating detector's thresholds, the `RuleSpec.Evidence` (`_evidence`) slot, `synth.WriteRuleCandidates`, or the `profile create --suggest-rules` flag | `synth/profile_gating.go` (the detector + every threshold constant, each with its reasoning at the declaration) + `synth/rules_evidence.go` (`RuleEvidence`) + `synth/rules.go` (`RuleSpec.Evidence`) + `synth/profile.go` (`ProfileOptions.SuggestRules`, `Profile.RuleCandidates`, the `gates.observe` hook in the row loop) + `synth/spec_io.go` (`WriteRuleCandidates`) + `synth/warning_summary.go` (the `rule candidate not considered` kind + the `gate level` thin subject) + `internal/cli/synth.go` (the flag) + `internal/cli/warnings.go` (`writeSuggestedRulesCaveat`, stderr only) + `skills/synth-structural-rules.md` (Detecting rules from the data) + `skills/session-bootstrap.md` (Synth-generation CLI flags) + `docs/src/cli/profile-create.md` + `docs/src/cli/flags.md` (flag section + command index) + CLAUDE.md "Synthetic data". **The written file must be the BARE rules array `--rules` consumes UNMODIFIED**, and the only assertion that proves it is a GENERATION round trip — a test that unmarshals the file into `[]RuleSpec` passes against a document whose predicates do not compile, whose targets name fields the derived spec does not declare, or whose `when` is silently never true, and all three are this package's own silent-inertness class. **`RuleSpec.Evidence` is THE ONE INERT SLOT and the inertness needs its own test** — a slot on a type whose other five members all execute is exactly where a later reader assumes execution. It must never be read by `compileRules`, the rule pass or `validateRules`, must never count as an action slot (a rule carrying only `_evidence` stays `PULSE_SYNTH_RULE_EMPTY`), and must stay `omitempty` so a hand-written rule is byte-identical without it. It rides the RULE rather than a sibling block because a wrapper object is refused by the loader by design and because evidence in a second document drifts the first time a candidate is deleted. **Thresholds are package CONSTANTS with their reasoning at the declaration, never `ProfileOptions` fields** (`gateHighNullRate` 0.98 / `gateLowNullRate` 0.02 / `minGateLevelSupport` = `MinPairObservations` / `maxGateLevels` 16 / `maxRuleCandidates` 20), matching `minVarianceExplained` and `minLevelObservations`: a threshold meant to mean the same thing across cohorts must not be tunable per run. They are TIGHT because skip logic is EXACT in the source — loosening them finds ordinary ASSOCIATION and proposes it as structure, and a rule built from a 10/30/50% null rate is applied unconditionally and wrong on most rows it selects, so the negative control must be an ASSOCIATION (a flat rate cannot be admitted at any threshold and therefore tests nothing). **Every gating candidate carries `OwnsNulls: true`, and `gateLowNullRate` is what makes that sound**: `P(null | open)` IS the residual `owns_nulls` zeroes, so an admitted candidate is within 0.02 of exact by construction. Emitting without it leaves the gate right about WHICH rows are absent and wrong about HOW MANY — the target's captured `null_rate` is a marginal already inclusive of the gate, so the two compose as `g + (1-g)*r` (measured on the motivating cohort: `regard` 0.2526 captured, 0.4369 generated; `people` 0.4273 captured, 0.7506 generated). A CO-MISSING candidate must NOT carry it — that slot discards its non-gate members' rates by copying the gate's decision. Assert the flag by the GENERATED MARGINAL, never by the key's presence. **Numeric gates are emitted through `round()`, uniformly, including `packed_bool`** — a bare `==` reads the pre-rounding float and under-fires silently (531 of 901 rows measured); assert it by FIRING, not by the predicate's spelling. **Every emitted `when` is compiled against the rule layer's OWN environment** (`rowExprEnv` / `rowExprOptions`) before it is written, never a hand-rolled identifier check — a field legally named `in` otherwise produces a candidate the loader refuses. **The detector rides the existing scan; assert it by COUNTING BYTES** the way `TestProfileModels_RidesTheExistingScan` does. **Absent the flag the profile document is byte-identical**, and with it ONLY `warnings` moves — `Profile.RuleCandidates` is `json:"-"` on purpose, because an unreviewed structural claim must not ride inside the document that drives generation. Two refusals are COUNTED rather than silent: a field over `maxGateLevels` is ABANDONED (never truncated — a partial level map makes every rate below it a rate over an arbitrary subset), and a gate whose only gated level is the null pseudo-level is co-missingness rather than value gating (every member of an N-field block reports the other N−1 that way; 3,932 on the motivating cohort) — the CO-MISSING detector in the row below proposes that block as one `null_together` candidate, so the two must not both stay silent about it. Thin candidates SHIP with their support attached, warned thinnest-first with a counted remainder. **A `set_null` candidate must survive `PULSE_SYNTH_RULE_FIELD_NOT_NULLABLE` against the spec the PROFILE derives**, not against a hand-built one: `SpecFromProfile` sets `Nullable` from the captured null rate and a gating target has nulls by construction, so the two agree — but the agreement is what makes the whole detect-then-generate loop work, and a fixture that builds its spec with `FieldSpec{Name, Type}` alone cannot see it break. `format_version` does NOT move and no error code is added, so neither golden moves | `TestSuggestRules_KnownGateYieldsTheCorrectTargetList`, `TestSuggestRules_EvidenceCarriesRatesSupportAndRowsAffected`, `TestSuggestRules_DeviationIsImpliedByTheSplitTest`, `TestSuggestRules_RidesTheExistingScan`, `TestSuggestRules_ThinSupportShipsWithItsSupport`, `TestSuggestRules_BoundedThinListingRollsUp`, `TestSuggestRules_HighCardinalityFieldIsNotAGate`, `TestSuggestRules_CoMissingGateIsCountedNotProposed`, `TestSuggestRules_PredicateSurvivesTheRuleCompiler`, `TestSuggestRules_UncompilablePredicateIsDroppedNotWritten`, `TestSuggestRules_CandidatesSurviveTheNullabilityRefusal`, `TestSuggestRules_WrittenFileGeneratesUnmodified`, `TestSuggestRules_NumericGatePredicateFiresOnEveryWireRow`, `TestSuggestRules_DocumentMovesOnlyItsWarnings`, `TestRuleEvidence_IsInert`, `TestWriteRuleCandidates_EmptyWritesAnEmptyArray`, `TestWriteRuleCandidates_RefusalsNameThePath`, `TestProfileCreateCLI_SuggestRulesRoundTripsThroughFromProfile`, `TestProfileCreateCLI_SuggestRulesLeavesTheDocumentAlone`, `TestSuggestRules_GatingCandidatesOwnTheirNulls` |
| The CO-MISSING detection surface — `synth/profile_comissing.go`, its thresholds, the `RuleEvidence.Block` slot, or the always-null finding | `synth/profile_comissing.go` (the detector + every threshold constant, each with its reasoning at the declaration) + `synth/rules_evidence.go` (`RuleEvidence.Block` / `RuleEvidenceBlockMember`, and the `omitempty` tags on the five gating-only slots) + `synth/profile.go` (`blocks.observe` in the row loop AND the candidate ORDER at the finish site) + `synth/warning_summary.go` (the `always-null column` kind + the `co-missing block` thin subject) + `internal/cli/warnings.go` (`writeSuggestedRulesCaveat`) + `internal/cli/synth.go` (flag prose) + `skills/synth-structural-rules.md` (Co-missing blocks) + `docs/src/cli/profile-create.md` + CLAUDE.md "Synthetic data". **THE EQUAL-RATE TEST ALONE IS INSUFFICIENT and that is the whole surface**: two independent fields can share a `null_rate` to sixteen digits and overlap by chance, so admission is IDENTICAL NULL PATTERN — the rate is only the grouping key. The discriminating test must assert directly that two independent fields sharing a rate are NOT emitted, and the falsification is to delete the pattern term from `classify` and keep the rate term. **The near-miss measure is the null-SET overlap (Jaccard), never row-level agreement**: two independent fields each null at 1% agree on 98% of ROWS, so any row-level threshold near 1 floods — the negative control must therefore include a LOW-rate independent pair, or swapping the measure leaves it green. **Accumulation is bounded and rides the existing scan**: a per-field bitset over `blockChunkRows` rows folded into an F x F co-null matrix by popcount, so memory is flat in the row count; assert both (bytes pulled, and buffer sizes across a cohort ten times longer AND past one chunk boundary). A per-field null-pattern HASH is the rejected alternative — it answers only yes/no, cannot measure a near miss, and trades exactness for a collision that fabricates a question block. Over `maxBlockFields` the detector is ABANDONED, never truncated. **An ALWAYS-NULL column is its own finding, named with its type, and is excluded from blocks** — two of them share a pattern perfectly, so only the exclusion keeps them out and a fixture with ONE such column cannot test it. **A near block is REPORTED, never emitted**: `null_together` has no dial for "almost" and nothing in it for an analyst to correct, unlike a gating candidate's `when`. **Emitted ORDER is applied order and a candidate WRITING a field another candidate READS is emitted BEFORE it** (`synth/profile_rule_order.go`, `ruleCandidateOrder`). The preference — gating, blocks, dependencies — is kept wherever nothing forces a move, and gate-before-block is right for a block holding a gate's TARGETS: identical patterns classify identically, so a gate takes a whole block or none of one, and a hand-narrowed `set_null` over part of a block is repaired by a block that follows it. That equivalence is FALSE for the field a gate READS — a block member that is a gate's `when` field moves the gate's own input after the gate read it, measured at 8,693 orphan rows of 20,000 on the motivating cohort and 0 after the reorder. `format_version` does NOT move and no error code is added, so neither golden moves | `TestSuggestBlocks_PatternNotMerelyRateYieldsTheBlock`, `TestSuggestBlocks_AlwaysNullIsItsOwnFinding`, `TestSuggestBlocks_PartialOverlapIsReportedWithItsAgreement`, `TestSuggestBlocks_IndependentPairsAreNotEvenReported`, `TestSuggestBlocks_EvidenceCarriesThePatternClaim`, `TestSuggestBlocks_RidesTheExistingScan`, `TestSuggestBlocks_MemoryIsBoundedByTheChunkNotTheCohort`, `TestSuggestBlocks_RankedLargestFirstAndBounded`, `TestSuggestBlocks_WideCohortIsAbandonedNotTruncated`, `TestSuggestBlocks_ThinBlockShipsWithItsSupport`, `TestSuggestBlocks_EvidenceKeepsTheGatingWireShape`, `TestSuggestBlocks_WrittenFileGeneratesUnmodified`, `TestSuggestBlocks_IndependentFieldsSharingARateAreNotABlock`, `TestSuggestBlocks_ComposeWithGatingCandidatesInOneFile`, `TestSuggestBlocks_DocumentMovesOnlyItsWarnings`, `TestSuggestBlocks_AlwaysNullColumnIsNamedWithItsType`, `TestProfileCreateCLI_SuggestBlocksRoundTripsThroughFromProfile`, `TestProfileCreateCLI_AlwaysNullColumnReachesTheTerminal` |
| The rule-candidate EMISSION ORDER — `synth/profile_rule_order.go` (`ruleCandidateOrder`, `ruleWrittenFields`, `ruleReadFields`), or the candidate assembly at the `finish` site in `synth/profile.go` | `synth/profile_rule_order.go` (the rule, its cost and its cycle policy stated at the top of the file) + `synth/profile.go` (the assembly comment) + `synth/profile_comissing.go` (`blockCandidateNote` — the per-candidate ordering advice a reader sees in isolation) + `internal/cli/warnings.go` (`writeSuggestedRulesCaveat`) + `skills/synth-structural-rules.md` + `docs/src/cli/profile-create.md` ("Emitted order is applied order") + CLAUDE.md "Synthetic data". **THE RULE IS WRITER-BEFORE-READER AND IT IS ABOUT A GATE'S SOURCE, not its targets.** The preference order (gating, blocks, dependencies) is a READABILITY choice and stays wherever nothing forces a move; the constraint overrides it. The equivalence argument that makes block-after-gate safe — identical null patterns classify identically, so a gate takes a WHOLE block or none of one — governs the fields a gate WRITES only. A block member that is a gate's `when` FIELD is the inverse hazard and the argument says nothing about it: the block rewrites the gate's own input after the gate read it, and the target the gate left present becomes an answer on a row whose screener is absent. Measured on the motivating cohort at **8,693 orphan rows of 20,000, 0 after the reorder**; a `set_expr` writing a gate's field is the same hazard reaching the other way across the file. **The falsification is the DETECTOR PREFERENCE ORDER as a negative control** — the same candidates in the order this replaced — because an orphan-count assertion alone passes on any order, including one that could never have produced the defect. Note the masking: a co-emitted PROXY gate on the dependency's own source reads a field nothing rewrites and nulls the same rows, so the dependency-to-gate hazard costs 0 rows until the analyst deletes the proxy, and a fixture that keeps it tests nothing. Minimum perturbation is half the rule and needs its own assertion: a candidate hoisted ahead of one the detectors ranked before it must have a dependent emitted after it. `format_version` does NOT move, no error code is added, neither golden moves, and the reorder is a PERMUTATION — no candidate's content changes | `TestRuleCandidateOrder_WriterPrecedesReader`, `TestRuleCandidateOrder_NoViolationSurvives`, `TestExprIdentifiers_ReadsOnlyFieldsAnotherCandidateWrites`, `TestSuggestRules_EmittedOrderLeavesNoOrphanRows`, `TestSuggestRules_EmittedOrderIsMinimallyPerturbed`, `TestSuggestBlocks_ComposeWithGatingCandidatesInOneFile`, `TestSuggestDeps_ComposeWithTheOtherDetectorsInOneFile` |
| The EXACT-DEPENDENCY detection surface — `synth/profile_dependency.go`, its thresholds, the `RuleEvidence.SourceField` / `.SourceLevels` / `.Dependency` slots, or the rendered `set_expr` form | `synth/profile_dependency.go` (the detector + every threshold constant, each with its reasoning at the declaration) + `synth/rules_evidence.go` (`RuleEvidenceDependency` / `RuleEvidenceMapValue` and the three dependency-only slots, all `omitempty` so the other two detectors' wire shape is unchanged) + `synth/profile_comissing.go` (`sameNullPattern` — the admission test is SERVED from E3-S2's co-null accumulator, never duplicated) + `synth/profile.go` (the `deps.observe` hook and the emit ORDER) + `synth/warning_summary.go` (the `dependency level` thin subject) + `internal/cli/warnings.go` (`writeSuggestedRulesCaveat` — the bounds are on STDERR as well as in the file) + `internal/cli/synth.go` (the flag prose) + `skills/synth-structural-rules.md` (Exact dependencies) + `skills/session-bootstrap.md` + `docs/src/cli/profile-create.md` + `docs/src/cli/flags.md` + CLAUDE.md "Synthetic data". **THE SEARCH BOUNDS ARE PART OF THE CONTRACT AND MUST BE STATED IN THE CANDIDATE FILE, ON STDERR AND IN THE DOCS** — `packed_bool`/`u4` targets, `categorical_*`/`packed_bool`/`u4` sources of at most `maxDepLevels` (16) observed levels, exactly ONE source — because a field missing from the file was NOT CLEARED, it was not examined, and an analyst who believes otherwise stops looking. Claiming generality the implementation does not have is worse than the bound. **BAND EDGES ARE DISCOVERED, NEVER ENCODED**: the standard NPS 9-10 / 7-8 / 0-6 definition is this detector's OUTPUT on the motivating cohort and a detector that hardcodes it is not a detector — the negative fixture must therefore carry NON-standard edges, since the motivating one agrees with a hardcoded answer by accident. **A PARTITION IS ONE CANDIDATE**, grouped by SOURCE: three rules would have to be kept consistent by hand and deleting one silently stops the remaining two partitioning. **ALL THREE E2-S4 REMEDIES RIDE THE EMITTED RULE** — `round()` on every numeric term (838 of 4,495 scored rows disagree without it, measured by GENERATION not by the predicate's spelling); NO separate normalisation rule (the rounding is inside each predicate, so nothing writes the source and the `!isnull` guard is unnecessary because the write does not happen); and `null_together` naming the SOURCE FIRST inside the SAME rule, where it is the last write (split out, the derivation runs afterwards and un-nulls every member — 1,505 orphan rows measured). **NO `when`**, which is both the true statement and what makes the targets PRE-CLAIM-ELIGIBLE (E2-S2) — assert that against `ruleClaims`, never a substring test, because `region == "west"` CONTAINS its target's name inside a quoted literal and does not read it. **ADMISSION IS IDENTICAL NULL PATTERNS** and an unavailable accumulator answers UNKNOWN rather than guessing; a differing pattern is REPORTED, because a `set_expr` clears the null mask and would un-null the target wherever its source is absent. **THE EXCEPTION COUNT MUST BE ORDER-INDEPENDENT**: a single first-observation slot per source level answers the EXACT question correctly and the NEAR question backwards when the minority value arrives first (80 exceptions reported for a 3-row miss, measured), so two slots per level are carried. **THE CONSTANT-TARGET GUARD MUST RUN BEFORE THE NULL-SHAPE REPORT** — otherwise every constant column is reported once per source as a dependency it is not — and it must be tested with a constant NUMERIC target, because a constant BOOLEAN one is refused three ways over and tests nothing. **EMITTED LAST**, after gating and blocks, so the derivation re-resolves its own block after every null-state rule; assert the order by BEHAVIOUR on a hand-narrowed gate (0 orphan rows last, 940 first), not by an index comparison. The detector rides the existing scan — assert by COUNTING BYTES. `format_version` does NOT move and no error code is added, so neither golden moves | `TestSuggestDeps_PartitionIsOneCandidateWithDiscoveredEdges`, `TestSuggestDeps_BandEdgesAreDiscoveredNotAssumed`, `TestSuggestDeps_AlmostDeterminedIsReportedNotEmitted`, `TestSuggestDeps_ExceptionCountIsOrderIndependent`, `TestSuggestDeps_NullShapeMismatchIsReportedNotEmitted`, `TestSuggestDeps_UnavailableCoNullMatrixRefusesRatherThanGuesses`, `TestSuggestDeps_CategoricalSourceYieldsMembership`, `TestSuggestDeps_ConstantColumnIsItsOwnFinding`, `TestSuggestDeps_UnrelatedFieldIsNotProposed`, `TestSuggestDeps_EvidenceCarriesTheMeasuredLookup`, `TestSuggestDeps_RidesTheExistingScan`, `TestSuggestDeps_HighCardinalitySourceIsAbandoned`, `TestSuggestDeps_ThinSupportShipsWithItsSupport`, `TestSuggestDeps_OneRuleWritesEachTarget`, `TestSuggestDeps_BoundedListingRollsUp`, `TestSuggestDeps_EmittedExpressionSurvivesTheRuleCompiler`, `TestSuggestDeps_UncompilablePredicateIsDroppedNotWritten`, `TestSuggestDeps_CandidatesArePreClaimEligible`, `TestSuggestDeps_DocumentMovesOnlyItsWarnings`, `TestSuggestDeps_WrittenFileGeneratesUnmodified`, `TestSuggestDeps_CategoricalDependencyFiresOnEveryWireRow`, `TestSuggestDeps_ComposeWithTheOtherDetectorsInOneFile`, `TestProfileCreateCLI_SuggestDependenciesRoundTripsThroughFromProfile` |
| `types.CanonicalHash` algorithm or `*.Hash` surfaces | `types/hash.go` + `synth/hash.go` | `TestCanonicalHash_*`, `TestSpecHash_*` |
| `StreamResult[T]` shape or `Pulse.ProcessStreamResult` / `Pulse.SynthStream` | `stream.go` + `descriptor/manifest.go` (Operations) | `TestProcessStreamResult_*` |
| `Pulse.Watch` / `Pulse.WatchDir` API or `WatchOptions` / `ChangeEvent` shape | `watch.go` + `descriptor/manifest.go` (Operations) | `TestWatch_*` |
| `FilterToFileRequest` / `FilterToFileResult` shape or dedup contract | `filter_to_file_request.go` + `descriptor/manifest.go` (Operations) | `TestFilterToFileWithRequest_*` |
| Manifest `CommandAnnotations` field or `Manifest.Operations` slot | `descriptor/manifest.go` + `descriptor/testdata/manifest.json` | `TestManifest_CommandAnnotationsPopulated`, `TestManifest_OperationsPopulated`, `TestManifest_AnnotationSemantics` |
| `Request.Crosstab` slot, `CrosstabSpec` / `MatrixPayload` / `CrosstabResult` shape, `CrosstabSpec.MarginAggregations` (auxiliary margin-only aggregations), or `AggregationType.MarginReducibility` | `types/crosstab.go` + `types/streamability.go` + `types/types.go` + `processing/crosstab.go` + `service/crosstab.go` + `descriptor/crosstab.go` + `descriptor/capabilities_crosstab.go` + `descriptor/manifest.go` + `mcp/bind.go` + `errors/codes.go` + `errors/fixup_metadata.go` + `docs/src/contract/payload-schema.md` + `skills/crosstab-guide.md` (a payload-reachable slot additionally regenerates `descriptor/testdata/payload-schema.json`; structural validation must be DETECTED once in `types` and merely RENDERED by the predict + execution validators, or the two drift on which specs they refuse). **`MarginAggregations` additionally carries its own row in CLAUDE.md's Update Demand table** — it reaches `Response.Components` as well as the request, so the accumulation arms, the record-ADMISSION rule, `skills/response-components.md` and CLAUDE.md "Output Format Contract" all ride with it; read that row before touching this one | `TestCrosstab_*`, `TestPredict_Crosstab_*`, `TestManifest_CrosstabCapability*`, `TestMCPSchemaBinding_Crosstab*` |
| `descriptor.Envelope.Request`, `EchoRequest` (Options + Predict), `ChainResponse.NormalizedRequest`, or `--echo-request` CLI surface | `descriptor/envelope.go` + `descriptor/predict.go` + `pulse.go` + `service/service.go` + `service/chain.go` + `types/chain.go` + `internal/cli/api.go` + `internal/cli/json.go` + CLAUDE.md "Output Format Contract" | `TestEnvelope_Request*`, `TestPredict_EchoRequest_Normalized`, `TestProcessChain_NormalizedRequest_PerStage` |
| `MatrixCell.Value` shape (scalar/rich), `AggregationType.MapValued` classifier, RichAggregator dispatch | `types/crosstab.go` + `types/streamability.go` + `processing/crosstab.go` + `processing/processor.go` + `processing/interfaces.go` + `descriptor/crosstab.go` + `descriptor/capabilities_crosstab.go` + `descriptor/manifest.go` + `mcp/bind.go` + `errors/codes.go` + `errors/fixup_metadata.go` + `skills/crosstab-guide.md` | `TestCrosstab_SetFrequencyCellEmitsMap`, `TestCrosstab_*MapValued*`, `TestManifest_CrosstabMapValuedCellAggregators` |
| `processing.CanFuseCrosstab` gate, `FusedCrosstabState`, or `StreamableGrouper.KeyFor` on a streamable Grouper | `processing/crosstab_fused.go` + `processing/crosstab_fused_gate.go` + `processing/grouper_*.go` + `service/crosstab.go` + `skills/crosstab-guide.md` + `skills/grouper-design.md` + CLAUDE.md "Execution modes" pointer | `TestFusedCrosstab_*`, `TestCrosstabFused_*`, `TestCanFuseCrosstab_*` |
| `pulse.Options.DecodeWorkers`, `service.canParallelDecode`, or `parallelDecodeRecordThreshold` | `pulse.go` + `service/parallel_decode.go` + `service/parallel_reduce.go` + `skills/cohort-schema-design.md` + CLAUDE.md "Build / Env" + CLAUDE.md "Execution modes" | `TestProcess_ParallelDecode_*`, `TestParallelDecode_PerfGate` |
| `Request.Overlays` / `Response.Overlays`, `OverlaySpec` / `OverlayLayer` / `OverlayCellLayer`, an `OVERLAY_*` kind, the `Extensions.OverlayKinds` registration slot, or an `OverlayOptions` knob | `types/overlay.go` + `types/overlay_streamability.go` + `processing/overlay.go` + `processing/crosstab.go` + `processing/crosstab_fused_gate.go` + `descriptor/overlay.go` + `descriptor/capabilities_overlay.go` + `descriptor/manifest.go` + `descriptor/predict.go` + `errors/codes.go` + `errors/fixup_metadata.go` + `mcp/bind.go` + `pulse.go` + `skills/overlay-system.md` + `skills/op-overlay-<kebab>.md` (atomic skill, one per `OVERLAY_*` kind) + `examples/overlays/` + `examples/library.go` + `docs/src/examples/library.md` + CLAUDE.md "Execution modes" pointer | `TestSkillsCoverAllOverlayKinds`, `TestManifestOverlayCapability*`, `TestOverlayCapabilities_*`, `TestValidateOverlay_*`, `TestOverlaySpec_*`, `TestOverlayLayer_RoundTrip`, `TestOverlayStreamable_*`, `TestStreamability_OverlaysKnown`, `TestStreamableWithOverlay_*`, `TestApplyOverlays_*`, `TestCrosstab_Overlay*`, `TestCanFuseCrosstab_OverlaysForceBuffered`, `TestCanonicalHash_Overlay*`, `TestProcess_Overlay*`, `TestMCPSchemaBinding_OverlayKindEnum`, `TestExtensions_OverlayKinds_NamingPolicy`, `TestOverlay_ShareOfRow_*` |
| `OverlayLayer.Warnings` slot shape, the `OverlayWarning{Code,Message,Details}` carrier, the dispatcher-stamped `Details["overlay_index"]` routing key, or the service-layer warning distribution helpers (`service.applyChainOverlays`, `service.applyComposeOverlays`) | `types/overlay.go` (`OverlayLayer.Warnings` slot + `OverlayWarning` shape) + `processing/overlay_chain_dispatch.go` (Chain dispatcher stamp) + `processing/overlay_compose_dispatch.go` (Compose single + multi-layer dispatcher stamps) + `service/chain.go` (Chain barrier route) + `service/compose_overlay.go` (Compose barrier route) + `skills/overlay-system.md` (Per-layer warnings section) + CLAUDE.md "Update Demand" row | `TestOverlayLayer_WarningsFreeByteIdentical`, `TestComposedResponse_OverlayFreeByteIdentical`, `TestSkillsCoverAllOverlayKinds` |
| `ComposedResponse{Responses,Overlays}` shape, the `Pulse.Compose` / `Pulse.ComposeParallel` facade return type (`*ComposedResponse`), the `pulse api compose --json` envelope wrapping (`data: ComposedResponse` object — not the legacy `[]*Response` array), or the `format_version` value (`"1.1"`) | `types/types.go` (`ComposedResponse` struct, byte-identity contract for the overlay-free path) + `pulse.go` (`Pulse.Compose` and `Pulse.ComposeParallel` doc comments + signatures) + `service/service.go` + `service/compose_parallel.go` + `service/compose_overlay.go` (post-slot fold, distributes per-layer `Warnings`) + `internal/cli/api.go` (Compose handler — envelope wrap, `--echo-request`, `--stream` per-row events) + `mcp/toolmeta/meta.go` (`pulse_compose` `DescCompose` text — `{responses, overlays}` shape) + `descriptor/envelope.go` (`format_version` constant) + `descriptor/testdata/manifest.json` (golden regen) + CLAUDE.md "Output Format Contract" (`--json` envelope + Compose envelope sub-block + `Response.Components` Compose-surface paragraph) + CLAUDE.md "The Update Demand" row + `skills/session-bootstrap.md` (step 7 Compose return shape note) + `skills/compose-requests.md` (per-slot Components contract + ComposedResponse envelope example) + `skills/tool-compose.md` (atomic MCP tool skill — refresh to mention `responses + overlays + warnings`) + `skills/overlay-system.md` (Compose-host fold + per-layer warnings cross-link) + `skills/streaming-and-watching.md` (streaming-vs-overlay limitation note) + `docs/src/cli/api-compose.md` (rewrite `responses[i]` shape note for the `.data` object) | `TestComposedResponse_OverlayFreeByteIdentical`, `TestOverlayLayer_WarningsFreeByteIdentical`, `TestClaudeMdMentionsFormatVersion`, `TestUpdateDemandTableCovers`, `TestSkillsCoverAllMCPTools` |
| `pulse.Options.SetInferenceMinPct`, `imports.Sidecar.ColumnTypeOverrides`, `pio.InferOptions`, `pio.ImportJob.SetDelimiters`, the `setWidth` helper, the delimited-cell heuristic, or the Arrow `LIST<UTF8>` mapping | `pulse.go` + `imports/imports.go` + `imports/manager.go` + `io/io.go` + `io/infer.go` + `io/import.go` + `io/arrow/types.go` + `io/jsonshared/values.go` + `io/ndjson/ndjson.go` + `io/jsonarray/jsonarray.go` + `encoding/field_type.go` + `internal/cli/fieldtype.go` + `skills/cohort-schema-design.md` | `TestInfer_*`, `TestConvertValue_SetU8*`, `TestImportJob_Set*`, `TestArrow_*`, `TestManager_Open_CSV_ColumnTypeOverrides*` |
| The inferred-import null-promotion contract: `io.ImportReport.PromotedFields`, `io.ImportJob.InferredSchema`, the `Run` promote-on-null / deferred-bitmap-buffer path, `ImportJob.Predict` nullability finalization, `ConvertJob` KeepPulseAt `InferredSchema` propagation, or `imports.Result.PromotedFields` | `io/io.go` (`ImportReport.PromotedFields`, `ImportJob.InferredSchema`) + `io/import.go` (`Run` promotion + interleave, `Predict` promotion) + `io/convert.go` (`InferredSchema` propagation to the re-import) + `imports/imports.go` + `imports/manager.go` (`Result.PromotedFields`) + `errors/codes.go` + `errors/fixup_metadata.go` (`PULSE_IMPORT_NULL_PROMOTED`) + `internal/cli/import.go` + `internal/cli/import_auto.go` (surface the warning) + `skills/cohort-schema-design.md` (Nullability promotion note) + `skills/tool-import.md` (`promoted_fields` output + gotcha) + `descriptor/testdata/manifest.json` (golden regen for the new code) | `TestImportJob_Promote_*`, `TestImportJob_ExplicitSchema_*`, `TestConvertJob_KeepPulse_PromotesOutOfSampleNull`, `TestCodesHaveFixups`, `TestManifestErrorCodesComplete` |
| The `io.SchemaAwareReader` optional interface (`PulseSchema() (*encoding.Schema, error)`), its `ImportJob.Run` / `ImportJob.Predict` type-assert bypass, or the authoritative-schema precedence rules (explicit `ImportJob.Schema` wins; `SampleRows` / `SetInferenceMinPct` / `SetDelimiters` / `ColumnTypeOverrides` inert; no `InferredSchema` null promotion; error fails, `(nil, nil)` declines) | `io/io.go` (interface + the four `ImportJob` slot doc comments + `InferredSchema`) + `io/import.go` (shared `readerSchema` resolver, `ImportJob.sourceSchema`, `Run` / `Predict` `authoritative` branch, the set-delimiter override) + `io/convert.go` (`ConvertJob.sourceSchema`, `Run` / `Predict` `authoritative` branch, `inferredSchema` suppression so the `KeepPulseAt` re-import never null-promotes) + `docs/src/internals/adding-io-format.md` (§4 Schema mapping — the implementer recipe) | `TestImportJob_NonSchemaAwareReader_ByteIdentical` (pre-change byte-identity baseline — the absent-implementation path), `TestImportJob_SchemaAwareReader_*`, `TestImportJob_Predict_SchemaAwareReader`, `TestConvertJob_SchemaAwareReader_*`, `TestConvertJob_Predict_SchemaAwareReader`, `TestConvertJob_NonSchemaAwareReader_Unchanged` |
| A registered I/O format: the `io/format` dispatch surface (format constant, `FromExt` extension mapping, `SupportedImport` entry, `NewReader` case), the CLI's two separate switches (`makeImportReader` in `internal/cli/import.go`, `newWriterForFormat` in `internal/cli/format.go`), or the manifest `Import` / `Export` capability blocks (`descriptor.ImportFormatCapability` — `Name` / `Extensions` / `SchemaSource` / `Export`) | `io/<fmt>/` (the adapter, with the house `var _ pio.X = (*Reader)(nil)` assertion block incl. every optional interface it implements) + `io/format/format.go` + `internal/cli/import.go` (`makeImportReader` case + `importFormatCmd` entry on `ImportCommand().Commands` + the `--format` usage strings) + `internal/cli/format.go` (`newWriterForFormat` + its `writerOptions` bag; an import-only format returns its OWN coded error, never the generic `unsupported format` — the extension IS recognised) + `internal/cli/export.go` (`exportFormatCmd` entry + `writerOptionsFrom`, when a writer exists) + `descriptor/capabilities_export.go` (`importCapability()` / `exportCapability()`, both alphabetised) + `descriptor/manifest.go` (`Manifest.Import` slot) + `mcp/contract.go` (`ImportIn.Format` jsonschema enum prose) + `mcp/toolmeta/meta.go` (`DescImport`) + `errors/codes.go` + `errors/fixup_metadata.go` (any format-specific codes) + `skills/cohort-schema-design.md` + `skills/tool-import.md` + the format's own topical skill when it carries a fidelity model of its own (`skills/spss-cohorts.md` for SPSS) + `skills/session-bootstrap.md` (only if the format adds a CLI flag — a new `pulse import <fmt>` subcommand alone does not qualify) + `docs/src/cli/import-<fmt>.md` (user-facing leaf page) + `docs/src/SUMMARY.md` + `docs/src/internals/adding-io-format.md` + CLAUDE.md "Architecture" `io/` line + CLAUDE.md "Manifest payload" + `descriptor/testdata/manifest.json` (golden regen) | `TestFromExt_Matrix`, `TestSupportedImport_ContainsSPSS`, `TestSupportedImport_EveryEntryConstructs`, `TestNewReader_SPSSContracts`, `TestNewReader_Rejections`, `TestManifestImportCapability`, `TestManifestImportCapability_AlphabetizedByName`, `TestManifestImportCapability_ExportFlagMatchesExportBlock`, `TestManifestImportCapability_MatchesFormatRegistry`, `TestFormatFromExt_SPSS`, `TestMakeImportReader_SPSS`, `TestImportCommand_HasSPSSSubcommand`, `TestNewWriterForFormat_SPSSBuildsTheWriter`, `TestRootManifestGolden`, `TestGoldensNotHandEdited` |
| The `io.CohortWriter` optional interface (`WriteCohort(ctx, CohortSource) (int, error)`), the `io.CohortSource` struct (`FS` / `Path` / `Includes` / `Labelled`), the `io.TargetWarningEmitter` optional interface (`Warnings() []*errors.CodedError`), the `TargetWarnings` slot on `ExportReport` / `ConvertReport`, or the `ExportJob.Run` dispatch that calls `WriteCohort` INSTEAD of its own row loop | `io/io.go` (both interfaces + `CohortSource` + the two report slots) + `io/export.go` (the post-`WriteHeader` dispatch + `targetWarnings`) + `io/convert.go` (the report lift) + the implementing adapter (`io/spss/writer.go` assertion block; a cohort writer MUST refuse `Includes` / `Labelled` with a coded error rather than ignore them — silently emitting every column in answer to `--include` is the failure the slot exists to prevent) + `internal/cli/export.go` (`finishExport`, which re-reads the warnings AFTER `Close` because a writer that encodes at `Close` has raised none of them when the job builds its report) + `internal/cli/convert.go` (same lift) + `skills/spss-cohorts.md` (Writing `.sav` — the CLI surface) + CLAUDE.md "Byte-layout invariants" (SPSS export) + `docs/src/internals/adding-io-format.md` | `TestWriter_SatisfiesTheOptionalInterfaces`, `TestExportJob_SPSS_NeverCallsWriteRow`, `TestExportJob_SPSS_RoundTripsThroughTheWholeAdapterPath`, `TestExportJob_SPSS_RefusesProjectionAndLabels`, `TestExportJob_SPSS_SidecarAbsentWarns`, `TestNewWriterForFormat_SPSSBuildsTheWriter` |
| The `io.CohortValidator` optional interface (`ValidateCohort(ctx, CohortSource) ([]*errors.CodedError, error)`), the `TargetWarnings` slot on `io.PredictReport`, or the `ExportJob.Predict` dispatch that consults it | `io/io.go` (interface + the `PredictReport` slot) + `io/export.go` (`ExportJob.Predict` type assertion; a nil / non-implementing Target MUST be predicted exactly as before) + the implementing adapter (`io/spss/writer.go` — `ValidateCohort` must re-run the write path's own non-data pass via the shared `planCohort` seam rather than re-state its checks, must mutate nothing on the Writer, and must NEVER refuse what the real export would accept) + `internal/cli/export.go` (`exportPredictCmd` — reads `--format`, builds the target on a MemMapFs + throwaway path so predict writes no file, mounts the target format's write flags, lifts `TargetWarnings` onto the envelope `warnings` array, routes a refusal through `writeCodedErrorEnvelope` so `errors[0].code` stays the export's own) + `skills/spss-cohorts.md` (Writing `.sav` — the predict paragraph) + CLAUDE.md "Byte-layout invariants" (Export predict is target-aware) + `docs/src/internals/adding-io-format.md` (§4 `io.CohortValidator`) + `docs/src/cli/export-spss.md` (Checking first) | `TestExportJob_Predict_NonValidatingTargetUnchanged` (the compatibility negative), `TestExportJob_Predict_ConsultsTheTarget`, `TestExportJob_Predict_CarriesTheSourceAndTheRefusables`, `TestExportJob_Predict_RefusalIsReturnedVerbatim`, `TestValidateCohort_NameRefusalMatchesTheExport`, `TestValidateCohort_StaleSidecarRefusalMatchesTheExport`, `TestValidateCohort_SanitizeNamesTurnsTheRefusalIntoAWarning`, `TestValidateCohort_WarnsWhereTheExportWarns`, `TestValidateCohort_WritesNothing`, `TestValidateCohort_LeavesTheWriterUsable`, `TestCliExportPredict_SPSSRefusesIllegalNames`, `TestCliExportPredict_NoFormatIsUnchanged`, `TestExportPredict_HasTheTargetFlags` |
| The `.sav` WRITE surface: `spss.WriterOptions` (`IgnoreSidecar` / `Uncompressed` / `Charset` / `SanitizeNames`), the CLI's `writerOptions` bag and `writerOptionsFrom` projection, the `pulse export spss` leaf, the `convert` leaf's write flags, or the name-sanitization rules (`spss.NameRename`, `sanitizeNames`, `DictionaryPlan.Renames`) | `io/spss/sidecar_read.go` (`WriterOptions`) + `io/spss/writer.go` + `io/spss/dict_names.go` (the derivation + collision rules) + `io/spss/dict_write.go` (`BuildDictionary`'s synth-path-only hook + `DictionaryPlan.Renames`) + `internal/cli/format.go` (`writerOptions`) + `internal/cli/export.go` (`exportSPSSCmd` + `writerOptionsFrom` + the shared flag-usage constants) + `internal/cli/convert.go` + `errors/codes.go` + `errors/fixup_metadata.go` (`PULSE_SPSS_NAME_SANITIZED`, `DetailSPSSRenames`) + `skills/spss-cohorts.md` + `skills/session-bootstrap.md` ("Target-format CLI flags") + `skills/cohort-schema-design.md` (SPSS import — the writable note) + `skills/tool-import.md` + CLAUDE.md "Byte-layout invariants" (SPSS export) + `descriptor/testdata/manifest.json` (golden regen when the error family moves) | `TestWriterOptionsFrom_MapsEveryFlag`, `TestExportSPSS_HasEveryWriteFlag`, `TestConvertHasTheWriteFlags`, `TestSanitizeNames_OffIsStillARefusal`, `TestSanitizeNames_DerivesLegalNames`, `TestSanitizeNames_CollisionSafe`, `TestSanitizeNames_IsDeterministic`, `TestSanitizeNames_ReportsEveryRename`, `TestSanitizeNames_NothingToDoIsSilent`, `TestSanitizeNames_SidecarPathUnaffected`, `TestCodesHaveFixups`, `TestManifestErrorCodesComplete`, `TestGoldensNotHandEdited` |
| The `--json` FATAL error envelope on the io leaves — `writeCodedErrorEnvelope` in `internal/cli/json.go`, or any `writeErrorEnvelope(w, "<PLACEHOLDER>", err.Error())` call on `import` / `import_auto` / `convert` / `export` | `internal/cli/json.go` + `internal/cli/import.go` + `internal/cli/import_auto.go` + `internal/cli/convert.go` + `internal/cli/export.go` + CLAUDE.md "Output Format Contract" (`--json` envelope bullet) | `TestWriteCodedErrorEnvelope_PreservesTheCode` |
| The `io.SourceWarningEmitter` optional interface (`Warnings() []*errors.CodedError`), its post-row-pass collection in `ImportJob.Run` / `ImportJob.Predict` / `ConvertJob.Run`, the `SourceWarnings` slot on `ImportReport` / `PredictReport` / `ConvertReport`, or the CLI routing that lifts them onto the `--json` envelope `warnings` array | `io/io.go` (interface + the three report slots) + `io/import.go` (`ImportJob.sourceWarnings`) + `io/convert.go` (`ConvertJob.sourceWarnings`) + `imports/imports.go` (`Result.SourceWarnings`) + `imports/manager.go` (the managed-import Result build — `pulse import auto` / `Pulse.ImportFile` / `pulse_import` do NOT return an `io.ImportReport`, so the channel is threaded twice) + `internal/cli/json.go` (`writeEnvelopeWithWarnings`, `writeSourceWarnings`) + `internal/cli/import.go` + `internal/cli/import_auto.go` + `internal/cli/convert.go` (call sites on both the JSON and text paths) + the emitting adapter (`io/spss/spss.go` assertion block) + `skills/spss-cohorts.md` (Warnings are load-bearing) + `skills/tool-import.md` + `docs/src/internals/adding-io-format.md` (§4 Non-fatal diagnostics) | `TestImportJob_SourceWarnings_Lifted`, `TestImportJob_Predict_SourceWarnings_Lifted`, `TestConvertJob_SourceWarnings_Lifted`, `TestSourceWarnings_AbsentInterfaceIsNil`, `TestSourceWarnings_EmptySliceIsNil`, `TestWriteEnvelopeWithWarnings_LiftsCodes`, `TestWriteEnvelopeWithWarnings_EmptyMatchesPlainEnvelope`, `TestWriteSourceWarnings_TextPath`, `TestSPSS_ImportSurfacesSourceWarnings`, `TestSPSS_ManagedImportSurfacesSourceWarnings`, `TestSPSS_ManagedImportCleanFileHasNoWarnings` |
| The `io.SidecarEmitter` optional interface (`WriteSidecar(fs afero.Fs, cohortPath string) error`), its post-cohort-write call in `ImportJob.Run`, the SPSS metadata sidecar document shape (`spss.Document` / `Payload` / `Fingerprint` / `Variable` / `Category` / `Missing`), `spss.SidecarSuffix`, `spss.SidecarFormatVersion`, or the flat/liftable payload constraint | `io/io.go` (interface) + `io/import.go` (`ImportJob.emitSidecar` + the `Run` call site, strictly after the cohort write) + `io/spss/sidecar.go` + `io/spss/sidecar_build.go` + `io/spss/spss.go` (assertion block) + `skills/spss-cohorts.md` (The metadata sidecar) + CLAUDE.md "Byte-layout invariants" (SPSS metadata sidecar) + `docs/src/internals/adding-io-format.md` (§4 `io.SidecarEmitter`) | `TestImportJob_NoSidecarEmitter_WritesNothing` (absent-path byte-identity), `TestSidecarPath_FollowsImportsConvention`, `TestSidecarSuffix_DoesNotCollideWithManagedImportSidecar`, `TestSidecar_PayloadIsSelfContained`, `TestSidecar_FingerprintsTheCohortNotTheSource`, `TestSidecar_CategoryTriple`, `TestBuildMissing_AllThreeShapes`, `TestSidecar_CoversTheDictionaryElementsPulseCannotHold`, `TestSidecar_RoundTripsThroughJSON`, `TestSidecar_IsDeterministic`, `TestFloat_NonFiniteSurvivesJSON` |
| The SPSS fidelity layer: the numeric-vs-categorical user-missing split (`io/spss/missing.go` / `missing_categorical.go`), the `<var>_missing` sibling naming + reason-dictionary ordering, the multiple-response derivation (`io/spss/mrset.go`), the derived-column registry (`spss.Derived` / `DerivedKinds()` / `DerivedFoldFor` / `Derived.Complete()`), or the `--spss-missing` / `spss.WithMissingMode` / `format.ReaderOptions.SPSSMissing` mode set | `io/spss/missing.go` + `io/spss/missing_categorical.go` + `io/spss/mrset.go` + `io/spss/derived.go` + `io/spss/sidecar_build.go` (`payload.derived`) + `internal/cli/import.go` + `internal/cli/convert.go` (the `--spss-missing` flag) + `io/format/format.go` (`ReaderOptions.SPSSMissing`) + `errors/codes.go` + `errors/fixup_metadata.go` (`PULSE_SPSS_DERIVED_NAME_COLLISION`, `PULSE_SPSS_MISSING_MODE_INVALID`, `PULSE_SPSS_CATEGORICAL_USER_MISSING`, `PULSE_SPSS_MR_SET_NOT_DERIVED`, `PULSE_SPSS_VALUE_COLLISION`) + `skills/spss-cohorts.md` (Missing values / Multiple-response sets / Derived columns) + `skills/cohort-schema-design.md` (SPSS import — the two-fact summary) + `skills/session-bootstrap.md` (`--spss-missing` under Source-format CLI flags) + CLAUDE.md "Byte-layout invariants" (SPSS derived columns) + `docs/src/cli/import-spss.md` + `descriptor/testdata/manifest.json` (golden regen when the error family moves) | `TestMissing_*`, `TestDerived_*`, `TestMRSet_*`, `TestSidecar_*`, `TestCodesHaveFixups`, `TestManifestErrorCodesComplete`, `TestSkillTokenBudget`, `TestGoldensNotHandEdited` |
| `pulse.Options.DisableComponents` (engine-level opt-out), `types.Request.DisableComponents *bool` (per-request override), `Service.SetDisableComponents` / `Service.effectiveDisableComponents`, `Processor.SetDisableComponents`, the per-execution-path emission gates (`processor.processStreaming` / `processStreamingGrouped` / `processStreamingTwoPass` / `processRecords` / `processGrouped` / `RunCrosstab` / `FusedCrosstabState.Finalize`), `service.finalizeMergedPartial` / `service.attachMergedRunComponents` `disableComponents` parameter, or the `--no-components` CLI flag on `pulse api process` / `process-chain` / `compose` | `pulse.go` (`Options.DisableComponents`) + `types/types.go` (`Request.DisableComponents *bool`) + `service/service.go` (`SetDisableComponents`, `DisableComponents`, `effectiveDisableComponents`) + `service/chain.go` + `service/crosstab.go` + `service/crosstab_fused.go` + `service/join.go` + `service/shard_reduce.go` (`finalizeMergedPartial` / `attachMergedRunComponents` signature) + `service/parallel_reduce.go` + `processing/processor.go` (`SetDisableComponents`, per-path emission gates, `aggregateWithComponents` `collectComponents` argument, `processGrouped` early return) + `processing/crosstab.go` (`RunCrosstab` emission gate) + `processing/crosstab_fused.go` (`FusedCrosstabState.disableComponents` field) + `processing/crosstab_fused_run.go` (state.disableComponents propagation) + `internal/cli/api.go` (`--no-components` flag on `apiProcessCmd` / `apiProcessChainCmd` / `apiComposeCmd`) + `skills/response-components.md` (Opting out section) + CLAUDE.md "Output Format Contract" (`Response.Components` opt-out paragraph) | `TestDisableComponents_*`, `TestProcessStream_DisableComponents`, `TestAPIProcess_NoComponentsFlag`, `TestComponentsUniversalFloor`, `TestClaudeMdMentionsComponentsContract` |
| `pulse.Options.TemplateDirs` slot, the `PULSE_TEMPLATES_DIR` loader / precedence rule, the template document model (`template.Template` / `Variable` wrapper keys), the target set (`template.AllTargets`), the variable type set (`template.AllVarTypes`), the substitution syntax (`$var` / `{{}}` / `$when`), the store's rescan-interval + per-file degradation policy, **the `template.Summary` listing contract (`Name`/`Description`/`Target`/`Variables`/`Path`/`Shadows`/`Broken`/`Error` — a new slot here is a public listing-shape change and needs its own companion rule)**, the `template.Rendered` shape (`Target` discriminant + the five typed pointers + `JSON`), or the five facade methods (`ListTemplates` / `GetTemplate` / `RenderTemplate` / `RenderTemplateRequest` / `ReloadTemplates`) | `pulse.go` (`Options.TemplateDirs` + the five facade methods + `wrongTemplateTarget`) + `template_loader.go` (`envTemplatesDir`, `resolveTemplateDirs`, `loadTemplateStore`) + `template/template.go` (document model, `Target`, `VarType`, `Summary`) + `template/validate.go` + `template/vars.go` + `template/render.go` + `template/decode.go` (`Rendered`, strict decode) + `template/store.go` (discovery, precedence, rescan, per-file degradation) + `errors/codes.go` + `errors/fixup_metadata.go` (`PULSE_TEMPLATE_*` family + `DetailTemplate`/`DetailVariable`) + `skills/request-templating.md` + `docs/src/library/request-templating.md` + `docs/src/SUMMARY.md` + CLAUDE.md "Request templating" + CLAUDE.md "Build / Env" (`PULSE_TEMPLATES_DIR`) + CLAUDE.md "The Update Demand" row + `descriptor/testdata/manifest.json` (golden regen if the error family moves) | `TestTemplatePackage_ImportBoundary`, `TestValidate_Matrix`, `TestResolve_AcceptanceMatrix`, `TestResolve_ProvenanceSelectsTheCode`, `TestRenderJSON_Matrix`, `TestRender_TargetMatrix`, `TestRender_NoFilesystemAccess`, `TestAllTargets_SpellingsAndCopy`, `TestAllVarTypes_SpellingsAndCopy`, `TestSummary_JSONShape`, `TestNewStore_*`, `TestStore_*`, `TestResolveTemplateDirs*`, `TestNew_Template*`, `TestNew_MalformedTemplateFailsNew`, `TestListTemplates`, `TestGetTemplate`, `TestRenderTemplate*`, `TestReloadTemplates`, `TestCodesHaveFixups`, `TestManifestErrorCodesComplete`, `TestClaudeMdMentionsAllEnvVars` |

**Not payload-reachable.** `Options` is not a payload type and nothing under `template/` lives in `types/`, so `descriptor.BuildPayloadSchema` never sees the document model, `Summary`, or `Rendered`. `format_version` stays `"1.1"` and `descriptor/testdata/payload-schema.json` is untouched by a templating change; only the manifest golden moves, and only when the `PULSE_TEMPLATE_*` error family changes. If a future change makes any template shape payload-reachable (e.g. a template slot on a `types` request root, or an MCP tool returning `Summary`), that change picks up the full public-payload-contract row above as well.

**`TestSkillsCoverAllCliLeaves` enforces NAMING, not quality.** The gate (`cmd/pulse/cli_leaves_test.go`, written at E5-S8 after being cited for several efforts without existing) walks the real `buildApp()` command tree and requires every ACTIONABLE command path — a command with a non-nil `Action`, which includes `pulse convert` alongside its `predict` subcommand — to appear verbatim somewhere under `skills/` or `docs/src/`. The command index in `docs/src/cli/flags.md` is the minimum home for a leaf with no dedicated page. It does NOT check that the mention is adequate, so the `skills/session-bootstrap.md` and `docs/src/cli/` companions in the rows below remain author judgement; what the gate catches is the case that actually happens — a leaf ships and nothing anywhere names it.

Defer the doc/skill update to "a follow-up PR" and the follow-up will not happen. Update in the same PR or do not merge.
