{
  "_comment": "INTENTIONAL LEAKAGE EXAMPLE. FEAT_TARGET_ENCODE runs without a preceding FEAT_TRAIN_TEST_SPLIT. Run with `pulse api predict --request 09_target_encode_leaky.json --json --strict` and confirm PULSE_FEAT_TARGET_LEAKAGE_RISK fires as an error. Drop --strict to see it as a warning.",
  "cohort": {
    "filename": "training_data.pulse",
    "data_dir": ".data"
  },
  "features": [
    {
      "type": "FEAT_TARGET_ENCODE",
      "field": "category",
      "params": {
        "target": "price"
      }
    }
  ],
  "aggregations": [
    {
      "type": "AGG_AVERAGE",
      "field": "TARGET_category",
      "label": "leaked_mean"
    }
  ]
}