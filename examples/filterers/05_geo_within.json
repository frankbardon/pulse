{
  "_comment": "Spatial containment filter: keep rows whose location falls inside a North America bounding polygon. WKT order is lon-lat; the ring must close on its first vertex.",
  "cohort": {"filename": "all_types.pulse", "data_dir": ".data"},
  "filterers": [
    {"type": "FILTER_GEO_WITHIN", "field": "location", "expression": "POLYGON((-130 25, -65 25, -65 50, -130 50, -130 25))"}
  ],
  "aggregations": [
    {"type": "AGG_COUNT", "field": "id", "label": "north_american_rows"}
  ]
}
