{
  "_comment": "Tier-1 two-proportion z-test on conversion rate by treatment. params.success names the dictionary entry that counts as a positive outcome on the field column. Variant gets a planted +5pp conversion lift across segments, so the z-test rejects with a clear effect size and CI.",
  "cohort": {"filename": "experiment.pulse", "data_dir": ".data"},
  "tests": [
    {
      "type": "TEST_PROP_Z",
      "field": "converted",
      "split_by": "treatment",
      "alpha": 0.05,
      "label": "conversion_lift_z",
      "params": {"success": "yes"}
    }
  ]
}
