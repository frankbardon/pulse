{
  "_meta": {
    "category": "aggregations",
    "description": "Null-state survey on the all_types.pulse u8 small_int field via AGG_NULL_COUNT paired with AGG_COUNT (non-null) for a complete present-vs-null contrast. AGG_NULL_COUNT consults the per-record null bitmap (Schema.HasBitmap()) so the count is exact even on fully-populated fixtures (returns 0 when no nulls — the canonical data-quality smoke shape).",
    "name": "null_count_contrast",
    "operators": [
      "AGG_COUNT",
      "AGG_NULL_COUNT"
    ],
    "tags": [
      "data-quality",
      "streaming-friendly"
    ]
  },
  "aggregations": [
    {
      "type": "AGG_COUNT",
      "field": "small_int",
      "label": "small_int_present"
    },
    {
      "type": "AGG_NULL_COUNT",
      "field": "small_int",
      "label": "small_int_null"
    }
  ],
  "cohort": {
    "filename": "all_types.pulse",
    "data_dir": ".data"
  }
}
